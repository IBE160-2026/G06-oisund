---
scope: Whole-system architecture for the accepted Bus Driver Assistant V1; extension boundaries for future data sources and operator integrations
purpose: Concise implementation consistency contract for Jonas and downstream BMAD/Codex agents, also readable by technical course reviewers; brief rationale, interfaces, diagrams and local/Docker delivery; no separate presentation
altitude: initiative
updated: 2026-09-23T20:15
---

- (decision) User requested interactive bmad-architecture for this project, with finalized Product Brief, PRD and UX as authoritative inputs. Coaching path follows the explicit user request; do not skip load-bearing decisions or silently draft them. Converse in Norwegian, write documents in English. No code or architecture technology choice is authorized by initialization alone.
- (constraint) Authoritative inputs read: briefs/brief-IBE160-2026-09-21/product-brief.md and addendum.md; prds/prd-IBE160-2026-09-21/prd.md (final, approved); ux-designs/ux-IBE160-2026-09-22/DESIGN.md and EXPERIENCE.md (final). Paths are relative to planning-artifacts. Inherit approved requirements; PRD explicit changes supersede brief background and explicit later user decisions consolidated in UX supersede affected PRD rules. Do not use generic precedence frontmatter to restore superseded behavior. No existing architecture run, application code or applicable AGENTS.md was found; initial git status was clean.
- (constraint) V1 is a tablet-adapted website with backend/database, primarily one operational user on Lenovo/Brave with phone tethering and lines 20,24,28,42. Preserve reviewed PDF/JPG/PNG import, manual physical-bus assignment distinct from vehicle duty, whole/split-day planning and confirmed revisions, actual-progress stop/trip selection, automatic Svipper-origin relevant notices, whole confirmed-day offline operation and restart, summary/PDF and isolated no-login fictional PC demo. Latest UX also includes operational FADDER/INSTRUKTOR with own and imported linked plans kept distinct, explicit role/context changes and acute FADDER takeover.
- (constraint) Inherited trust/lifecycle constraints: planned, observed, manual, stale, unavailable and simulated evidence remain distinct; manual trip authority and notice identity/seen/acknowledged versions survive recovery; source timestamps differ from retrieval. Reliable-motion driver mode permits notice headings only; stationary detail and acknowledgement supersede former <=6 km/h and 30-second rules. Preserve genuine-startup and five-minute GPS-loss exceptions and established timer state through restart; guiding controls and direct theme/GPS-loss stop exceptions remain distinct. No silent switch from recovered driving to guiding.
- (constraint) Own-day data expires seven days after confirmed end/abort or planned final end if never ended, without inferred completion. Accompanied-person uploaded sources are deleted immediately after successful import; retain necessary separate interpreted context during own day, only actually accompanied evidence for final summary and discard remainder at ending. First post-end summary review can manually confirm uncertain activities; subsequent retained access is read/export-only. Private records and public demo remain isolated. Do not publish real operational identifiers.
- (constraint) V1 boundary inherited from PRD and latest UX: speed limits, weather, meeting-bus warnings, calculated deadhead routes, general multi-driver operations, tour/charter support, product AI and direct Selfservice/operator integration are not required V1 capabilities. User now requires extensibility for later data sources/operator integrations; this does not authorize building them in V1 or reopening accepted UX scope silently. Future assignments must not universally depend on route numbers, fixed stops or Entur.
- (event) Read existing PRD source-feasibility research as historical evidence only: candidate Entur transport for Svipper-origin notices and timetables, coverage/update/end semantics and target-device capability remain untested. No current web validation or live feed/device test performed in this architecture run. Before binding technologies/starters, verify current official sources. Capacity remains 40-160 hours, upper bound not assumed; no implementation feasibility claim.
- (question) Activation deliverable question sent: Is the architecture spine the only deliverable, or is additional explanation needed; if so, for which audience and purpose? Await user response before choosing additional artifacts. Next coaching work should expose architecture alternatives, V1/extension boundaries, stack/starter and runtime/data ownership, then imports/integrations, offline/auth/retention, operational envelope and evidence gates. No paradigm, stack or boundary decision has been adopted yet.
- (decision by user) Deliver one concise decision-oriented Architecture Spine for Epics & Stories and subsequent implementation/maintenance by Jonas and BMAD/Codex agents. It must also explain V1 boundaries, components/data flows, core technology rationale, shift import, Entur data, GPS, disruptions, offline behavior, freshness/uncertainty, extension to new sources/operators without Tide/Tromso lock-in, and local/Docker-based operation and delivery. Include necessary diagrams, decisions and interfaces. No separate presentation or extensive nontechnical artifact. User requires brief rationale in the deliverable; this overrides skill default placing all rationale solely in memlog. Stack and detailed deployment choices remain open.
- (direction) First architecture proposal, not adopted: modular monolith backend with lightweight Ports and Adapters, provider-independent domain rules and a web client capable of the required offline operation. Alternatives to present: conventional layered app with fewer initial abstractions; modular monolith with explicit domain/integration boundaries; independently deployed services with additional deployment/coordination cost. Recommend modular monolith for solo V1 and controlled source/operator extension. Exact modules, data ownership, client/server split, stack and runtime topology remain subsequent decisions. Pattern rationale reality-checked against primary descriptions on 2026-09-23: https://alistair.cockburn.us/hexagonal-architecture and https://martinfowler.com/bliki/MonolithFirst.html . These conceptual sources do not validate project implementation or data-source feasibility.
- (question) Next user decision: assess the proposed modular-monolith plus Ports-and-Adapters direction and its small upfront boundary/interface cost versus the simpler layered alternative. Present realistic alternatives and application-specific rationale before adoption. User has already resolved deliverable scope; do not repeat that question. No architecture AD adopted yet.
- (decision by user) AD-1 [ADOPTED] - Modular monolith with Ports and Adapters. Binds: all V1 capabilities and external integrations. Prevents: independently deployed V1 feature services, provider-format coupling in domain logic and unnecessary internal abstraction. Rule: one cohesive backend application, web client and database; modules retain explicit responsibilities. Create ports primarily at real external boundaries such as shift import, transit data and disruption data, only where V1 needs interfaces. Ordinary internal logic does not require speculative ports. Rationale: solo delivery with clear integration boundaries and manageable operational complexity. User explicitly approved the proposal and these limits.
- (constraint by user) V1 integration scope clarification: Tromso is pilot configuration/data, never hardcoded into the domain model. Tide is NOT a concrete V1 source; V1 uses generic image/PDF import. Tide Selfservice is only a possible optional future operator adapter if access becomes available. NVDB, DATEX, weather, speed limits and meeting-bus features must NOT be implemented in V1; this tightens earlier optional-extension language. Provide natural future attachment boundaries without building future integrations or defining speculative interfaces. Define only interfaces required for V1 now. Next coaching decision is web-client/tablet versus backend responsibility and active-day ownership.
- (event) Browser feasibility documentation checked on 2026-09-23: https://developer.mozilla.org/en-US/docs/Web/API/Service_Worker_API describes offline resource caching and secure-context requirements; https://developer.mozilla.org/en-US/docs/Web/API/IndexedDB_API describes asynchronous transactional structured storage; https://developer.mozilla.org/en-US/docs/Web/API/Storage_API/Storage_quotas_and_eviction_criteria documents best-effort versus persistent storage and eviction; https://developer.mozilla.org/en-US/docs/Web/API/Geolocation_API documents permission-controlled device positioning in secure contexts. These support the proposed architecture, not proof of Lenovo/Brave performance. No library/storage implementation selected. Require actual-device recovery/eviction and GPS evidence; HTTPS provisioning for tablet access belongs to deployment decision, since desktop localhost exceptions do not make a remote LAN HTTP address secure.
- (direction) Proposed AD-2, awaiting user: web client owns the active-day operational state both online and offline; backend owns authenticated access, import interpretation, source adapters/normalization and database persistence of validated synchronized records. Client handles actual position/progression, manual pin, role/linked-person context, movement controls/timers, acknowledgement, local corrections/completion and daily summary. Cache all required confirmed own/linked plans, available stop lists and received notices for the whole day with explicit missing/stale states. Backend finds timetable candidates and receives user-confirmed plan revisions; it never silently overrides active state, selects trips from schedule, or marks observed completion independently. New file interpretation and uncached source requests require connectivity under this proposal; manual edits and already-loaded matching remain local. Local PDF rendering proposed alongside summary so export can be independent of backend availability; library/format decision remains open. Demo reuses applicable rules with isolated fictional inputs/state.
- (direction) AD-2 proposal consistency limits: one controlling browser/device per active day, including a single mutating tab; do not implement concurrent cross-device day editing in V1. Local state changes and a minimal pending-sync queue persist together, with stable operation identities and ordering/revision evidence; backend validates access/invariants, acknowledges receipts and handles retries without duplicate effects. Do not implement general event sourcing or a CRDT platform. Separate external-source updates from operational-state synchronization. Reconnection merges validated receipts and source revisions without overwriting local manual progress; no last-write-wins based on timestamps alone. No raw GPS streaming/tracks required; retain only necessary derived operational/correction evidence under existing retention. Local and server stores follow the same expiry; stale synchronization must not recreate deleted data. Concrete lock/recovery, sync schema, offline authorization/revocation and closed-browser expiry limitations remain explicit subsequent decisions.
- (question) Next user decision to present: accept proposed client-owned active-day operation and backend-owned import/sources/access/persistence, with one controlling client per active day and no concurrent multi-device editing in V1? Tradeoffs to explain: server-driven runtime would need offline duplicate execution and ownership handover; generic multi-writer synchronization adds conflict complexity. Proposed choice keeps the same local runtime during connectivity changes, at the cost of client persistence/sync work and potential loss of unsynchronized evidence if local storage/device is lost. AD-2 not yet adopted.
- (decision) AD-2 [ADOPTED] - Client-owned active operations with transactional offline state and explicit synchronization authority. Binds: active-day runtime, persistence/recovery, sync, import/source updates, role/movement rules and summaries. Prevents: network loss stopping a prepared day; UI acknowledging changes that were not stored; duplicate sync effects; server/source refresh silently overwriting manual corrections; hidden concurrent-writer conflicts. Rule: the active client is operational authority for the ongoing working day online and offline. Backend is authoritative for access, external source data and synchronized history; it validates submitted changes. Persist active operational changes locally BEFORE showing the action as completed, using an atomic transaction for working state and an outbox of idempotently processed events. Use IndexedDB for working data and Service Worker/cache for the application shell; browser storage is not guaranteed durable. Show actual offline readiness and warn when required content is missing. New imports and uncached source retrieval may require network; the confirmed, prepared whole working day must operate offline. User approved the preceding responsibility split, including local summary/PDF and backend import/source access; concrete packages and contracts remain later choices.
- (decision) AD-2 clarification [ADOPTED]: one controlling client per active working day is both a V1 usage constraint and a conflict rule. Online backend can record client identity plus revision/write lock. Two disconnected clients cannot physically be prevented from editing independently; detect such divergence on synchronization and never silently merge or overwrite either branch. Preserve manual corrections and explicit driver choices across all subsequent source updates. Explicit access controls remain server-enforced; operational client authority does not authorize writes to unrelated records. Exact lock lifetime, takeover/recovery, offline permissions and user-visible conflict resolution remain later decision points.
- (constraint) Latest user retention clarification: raw shift images/PDFs are retained only as long as processing requires and are deleted under the agreed privacy rules. Do not infer blanket seven-day raw-source retention from the older UX wording. Accompanied-person source deletion immediately after successful import remains binding; minimize own-source retention too. Define precise processing/review/failure/cancellation deletion checkpoints at the import/privacy decision without keeping files merely for history. Derived own-day/summary retention remains the agreed seven-day rule.
- (question) Optional stack-preference question sent asynchronously: any experience or constraints for language, framework or database? Compare TypeScript throughout versus Python backend plus TypeScript web client while awaiting answer. Next load-bearing decision is stack/starter; no stack beyond the user-selected IndexedDB and Service Worker/cache has been adopted.
- (version) Stack research on 2026-09-23 (candidates, not adopted): Node official release page https://nodejs.org/en/blog/release lists 24.21.0 as latest LTS and 26.10.0 Current; https://github.com/nodejs/Release identifies 24.x Active LTS. React https://react.dev/versions lists stable 19.3.0 (2026-09-09). Vite official starter create-vite@9.2.1 is latest generator release at https://github.com/vitejs/vite/releases/tag/create-vite@9.2.1 ; released react-ts template https://raw.githubusercontent.com/vitejs/vite/create-vite@9.2.1/packages/create-vite/template-react-ts/package.json contains React ^19.2.8, TypeScript ~6.0.2 and Vite ^8.3.0. Main-branch template now has React ^19.3.0, so do not conflate generator release defaults with main or treat ranges as installed lockfile versions. Fastify stable latest is 5.12.5 at https://github.com/fastify/fastify/releases/tag/v5.12.5 ; official LTS policy supports active Node LTS lines. Candidate core: TypeScript 6, React 19, Vite 8, Node 24 LTS and Fastify 5. Exact dependency versions/compatibility must be fixed when adopting and bootstrapping, not silently inferred from main.
- (event) Official starter fit researched: https://vite.dev/guide/ documents react-ts template, also linked in https://react.dev/learn/build-a-react-app-from-scratch . Fastify CLI https://raw.githubusercontent.com/fastify/fastify-cli/main/README.md documents generate --lang=ts with plugins/routes/test; guessed templates/ts/package.json path returned 404, so no generated backend dependency defaults have been inspected or installed. Full Stack FastAPI alternative https://fastapi.tiangolo.com/project-generation/ and https://github.com/fastapi/full-stack-fastapi-template bundles Python API, React/TypeScript/Vite, generated client, PostgreSQL, authentication, tests and Docker Compose; current template backend pyproject requires Python >=3.14,<4 and FastAPI >=0.141.1,<1, with SQLModel/Alembic/Pydantic. Template conveniences do not settle our offline/outbox/authentication requirements; adapt any selected starter to AD-1/2 and accepted UX. No starter executed.
- (event) Additional bounded fit checks for future decisions, not package selections: PDF.js https://mozilla.github.io/pdf.js/ is a PDF parsing/rendering candidate; Tesseract.js README https://raw.githubusercontent.com/naptha/tesseract.js/master/README.md confirms Node/browser image OCR and explicitly no PDF input support, so scanned PDFs need rendering and structured shift interpretation remains unvalidated regardless of language. PostgreSQL versioning https://www.postgresql.org/support/versioning/ lists 18.6 supported, 19 still beta; transaction and constraints docs confirm needed primitives. https://docs.docker.com/compose/ documents local multi-container application definition. DB/OCR/ORM/Service Worker-helper/IndexedDB-wrapper packages are not adopted yet.
- (direction) Next proposal (AD-3 pending): TypeScript throughout, React/Vite web client from the official react-ts starter and Node 24 LTS/Fastify 5 modular backend with official TypeScript scaffold. Rationale: client-heavy operational rules already required by AD-2; one language permits shared wire contracts/pure validation with explicit server revalidation, while UI/framework/source details stay out of domain rules. Cost: assemble database/authentication/Docker wiring, unlike more complete FastAPI full-stack starter. Real alternative: Python/FastAPI backend plus React/Vite/TypeScript web client from official Full Stack FastAPI Template, with two language ecosystems and generated API contracts. Prefer Python if user's experience materially reduces delivery risk. No assumption about prior language experience or approval; optional preference question remains unanswered at this point. Present alternatives and recommendation, then await choice. Database/data contracts and import/sync/access/deployment details remain future decision points.
- (decision) AD-3 [ADOPTED] - React/Vite/TypeScript web client and Python/FastAPI modular backend monolith. Binds: application language/framework split and API boundary. Prevents: adopting the proposed all-TypeScript/Fastify backend against the course direction, or moving/duplicating the client operational runtime into Python merely to align languages. Rule: use React/Vite plus TypeScript in the web client and Python/FastAPI for the one backend; AD-2 remains unchanged. Backend validates all received data. Define explicit API contracts; TypeScript client/type generation from FastAPI OpenAPI is a candidate to evaluate, not yet a chosen generator. Rationale: user reports textbook chapter 2 uses JavaScript/TypeScript with Node.js on the frontend and Python/FastAPI in its example backend and wants to follow this strong educational direction. This is a user-reported preference, not a verified mandatory submission requirement. No existing codebase or strong personal framework/database experience constrains the choice.
- (constraint) AD-3 starter scope: user asks to evaluate official Full Stack FastAPI Template and use it only where its prepared setup saves work for this V1. Do not inherit unneeded product features, login/session choices or deployment services merely because bundled. Starter adaptation must obey approved UX, private operational access/public isolated demo, AD-1 module boundaries and AD-2 client operational authority. Database and concrete data contracts are the next interactive decisions; no database preference supplied. Prior all-TypeScript recommendation is superseded.
- (event) AD-3 evidence and starter assessment, 2026-09-23: FastAPI latest stable is 0.141.1 per https://github.com/fastapi/fastapi/releases/tag/0.141.1 . Inspected official template master compose.yml, compose.override.yml, compose.deploy.yml, frontend/openapi-ts.config.ts, frontend/src/hooks/useAuth.ts and backend/app/core/security.py through raw.githubusercontent.com/fastapi/full-stack-fastapi-template/master/. Current template supplies PostgreSQL 18, build/test/health-check scaffolding and generated TypeScript SDK; includes Traefik/Adminer and development Mailpit, registration, JWT tokens stored in localStorage and email recovery. These defaults are not our accepted private-access/offline-session design. Assessment: selectively reuse useful build/test/API-generation and later chosen migration setup, with our modular structure and UX; do not wholesale adopt template application/auth/deployment. Exact extraction is an implementation task after dependencies are selected, and no application scaffold or dependency install has occurred.
- (direction) AD-4 proposal, not adopted: PostgreSQL 18 for backend persistence (current stable minor 18.6); IndexedDB remains client working store from AD-2. Alternative SQLite is technically sufficient for this low-volume pilot and has transactions/constraints, with simpler one-file deployment but one writer at a time. Prefer PostgreSQL for coordinating source ingestion, sync revisions/receipts and cleanup with a conventional relational schema and same DB semantics in development/integration tests/deployment; it also matches the official starter. Cost is one additional local/Docker service with a persistent volume and migration/backup/deletion discipline. This is not justified by invented V1 traffic or general multi-driver scope. Keep primary identifiers, ownership/relationships, revisions and uniqueness relational; typed structured snapshots/event payloads may use JSON where useful. Never store the entire application solely as an unvalidated JSON blob. Do not claim PostgreSQL alone solves synchronization or that SQLite cannot enforce consistency. Sources: https://www.postgresql.org/support/versioning/ ; https://www.postgresql.org/docs/18/ddl-constraints.html ; https://www.postgresql.org/docs/18/sql-insert.html ; https://www.sqlite.org/whentouse.html .
- (direction) AD-5 contract proposal, not adopted: explicit FastAPI request/response DTO schemas generate OpenAPI 3.1, which generates the TypeScript API client/types at build time; backend validates received data and authorization. Database row models and network DTOs are distinct; do not expose database internals or reproduce client runtime in Python. Generator choice/version remains implementation seed pending selected scaffold. Candidate conventions: /api/v1; stable operation IDs; snake_case wire fields; opaque application UUIDs creatable offline, external identifiers namespaced as source references rather than primary app identity; service_date plus explicit IANA time_zone and unambiguous scheduled date/times (overnight preserved), actual instants UTC; unknown values remain explicit null/status, never zero/empty facts. Source freshness, semantic certainty and origin are separate dimensions. Unknown major schema versions do not overwrite the active cached day. Contract regeneration/drift check belongs to build verification. Official basis: https://fastapi.tiangolo.com/advanced/generate-clients/ and https://fastapi.tiangolo.com/tutorial/response-model/ .
- (direction) AD-5 proposed required contract groups: ImportDraft (draft_id, target_plan_id, import scope whole/part/additions, interpreted activities, field provenance, issues; no activation before confirmation); Workday (id, authenticated owner, lifecycle, planned_final_end, ended_at, expires_at) plus PlanRevision (plan_id, own/accompanied kind, revision, service_date/time_zone, stable activity IDs, work parts and explicit accompaniment links); OperationalState (workday/context/plan/trip identifiers, actual driver/guiding role, manual trip pin, stop progress, qualified sensor/outage state and per-notice-version seen/acknowledged state) stays client operational authority; NoticeVersion plus SourceStatus (notice identity/version/applicability/validity, nullable source_updated_at, backend fetch/last-success evidence, separate client received time; changing retrieval time does not create a new content version); OfflineBundle (schema_version, confirmed plan revisions, stop-list snapshots, notices/statuses, expiry and required-content manifest; client verifies stored contents and app shell before declaring readiness, known missing-stop fallback must remain distinguishable from failed preparation); SyncBatch plus SyncReceipt (stable batch/event identities, workday_id, client_id, writer generation, expected server revision, ordered typed events and machine-readable result). Keep own and linked plans distinct inside the same private own day; linked imports are not another account's records. Detailed SQL columns/endpoint inventory deferred to implementation within these contracts.
- (direction) AD-5 proposed sync semantics to explain with revision example: one stable in-flight batch per day; expected_revision applies to batch base, each new accepted batch atomically commits domain records, event-id deduplication/receipt and next operational day revision. Events carry event_id, client_sequence, event_type, occurred_at and typed payload; clock timestamps never determine writer precedence. Repeated identical batch returns prior receipt even if revision has advanced; mismatched reuse of an ID is rejected. Authentication/ownership validated before deduplication; writer generation and expected revision govern new changes. No partial acceptance within a new batch; conflicts retain local unacknowledged work for explicit resolution. External source polling has separate source/version state and does not increment operational day revision or overwrite corrections. Unexpired authorized day must already exist; late sync cannot recreate expired/deleted work. This specifies transaction/idempotency boundaries, not a complete event-sourced domain engine. Client takeover, offline authorization and exact conflict UX still require later decisions.
- (question) Next two explicit proposals await user: AD-4 PostgreSQL backend versus SQLite; AD-5 schema-generated API contracts and required V1 contract boundaries/transaction semantics. Present them as proposals with small concrete revision example, not silently adopted decisions. User approved Python/FastAPI AD-3 and authorized template evaluation, not database or generator selection. No pending question remains about prior language experience; user's textbook direction answered both prior experience and stack choice.
- (decision) AD-4 [ADOPTED] - PostgreSQL 18 for backend persistence and integration tests. Binds: all backend storage, schema migration and integration-test setup. Prevents: PostgreSQL-only transactional behavior being validated against SQLite or mixing incompatible backend stores. Rule: use PostgreSQL 18 in development/integration tests/delivery, with relational ownership, stable identifiers, revisions and uniqueness; constrained structured payloads may use JSON. IndexedDB remains the client store. Rationale: conventional transactions for sync receipts/revisions, source ingestion and cleanup with an acceptable additional Docker service; SQLite was viable at pilot scale but not selected. PostgreSQL 18.6 was the current verified maintenance release at authoring.
- (decision) AD-5 [ADOPTED] - Six conceptual contract boundaries, schema-generated API and atomic idempotent synchronization. Binds: client/backend data exchange, external-source normalization, persistence and recovery. Prevents: language-specific model drift, false event completion, duplicate sync effects, hidden concurrent edits and conflating plan/source/actual outcomes. Rule: retain six conceptual groups: ImportDraft; Workday/PlanRevision; OperationalState; NoticeVersion/SourceStatus; OfflineBundle; SyncBatch/SyncReceipt. Explicit FastAPI request/response schemas produce OpenAPI and build-time TypeScript client/types; backend validates data and authority. Commit each accepted batch's changes, receipt and next operational revision atomically. Identical retry returns prior receipt; concurrent incompatible work yields conflict without silent overwrite. Source polling/versioning remains separate. Rationale: preserves AD-2 client authority while keeping two-language boundaries consistent. User asks to fix necessary identity/revision/time/error conventions now and defer full fields/tables to stories/implementation.
- (constraint) AD-5 user clarification: OperationalState is primarily the local client operational model, not a mandatory full-state API object on every change. Transmit only typed events and minimal state/checkpoints actually required for synchronization and recovery. On conflict, unsynchronized work stays local and awaits an explicit resolution path; never silently merge or overwrite it. Existing retention limits still apply. Checkpoint payload/cadence and concrete SQL/DTO fields are implementation/story detail within the consistency rules.
- (decision) AD-5 minimum identity/revision conventions established under user's instruction to fix them now: application-generated opaque UUIDs for workday, plan, activity, tracking context, client, batch and event; authenticated ownership derives from access context, never client_id alone. Notice identity/version is normalized stable identity plus opaque version key backed by source evidence; external IDs remain namespaced source references. Plan revision and operational server revision are separate monotonic integers; source versions are separate opaque values. Backend-issued writer_epoch identifies the controlling-client generation. A new batch carries schema_version, batch_id, workday_id, client_id, writer_epoch, expected_revision and ordered typed events; each event has event_id, client_sequence, occurred_at and necessary payload only. Client timestamps never determine conflict precedence. Retried identities must refer to identical content. No detailed table layout or full OperationalState transfer is bound.
- (decision) AD-5 minimum time/error conventions established under user's instruction: service_date remains a local operating date with explicit IANA time_zone; scheduled endpoints must preserve actual calendar dates/overnight sequence, and ambiguous local times remain unresolved for correction. Actual/reporting instants use UTC ISO 8601; occurred_at is client-reported occurrence time, received_at is backend receipt, and receipt must not replace occurrence evidence. Separate nullable source_updated_at, source_fetched_at/last-success evidence and client_received_at. Fixed expires_at derives from the agreed ending/planned-end policy, never polling/export/retry. Sync outcomes: accepted or already_applied return the committed receipt/revision; 409 revision_conflict, writer_conflict or idempotency_key_reused; 422 invalid_payload or unsupported_schema; 401 authentication_required and 403 access_denied; 410 workday_expired/gone after authorized existence check; temporary/unknown-delivery failures keep the same batch for retry. Authentication/ownership checked before returning prior receipts; deduplication precedes rejecting a legitimate identical retry solely for stale revision. Atomic rejection has no partial domain write. Only a confirmed matching receipt clears acknowledged outbox events; conflicts preserve pending work. Expired/deleted days cannot be recreated by sync. More detailed endpoint/field/error copy is deferred to stories.
- (question) Next coaching decision is AD-6: backend shift-import interpretation approach and transient raw-source lifecycle, preserving existing shared PDF/JPG/PNG review, explicit plan target/scope and source-vs-interpretation UX. Compare locally operated PDF text extraction plus OCR and parsing with an external document/vision service; do not silently choose a paid service, product AI dependency, or automated operator access. Need an explicit interpretation of source preview lifetime versus immediate deletion after import, followed by later source/timetable/GPS/auth/deployment decisions.
- (version) AD-6 candidate research verified 2026-09-23: pdfplumber 0.11.10 is the current tagged release (https://github.com/jsvine/pdfplumber/releases/tag/v0.11.10); official README says it extracts PDF text/layout/tables and works best on machine-generated PDFs (https://github.com/jsvine/pdfplumber). Tesseract 5.5.3 is the current tagged release (https://github.com/tesseract-ocr/tesseract/releases/tag/5.5.3); official README supports PNG/JPEG, text/TSV output and notes image-quality sensitivity and no PDF document input (https://github.com/tesseract-ocr/tesseract/blob/main/README.md). Scanned PDF pages therefore need rendering before OCR. These are candidates, not adopted libraries or evidence of accuracy on pilot files.
- (direction) AD-6 PROPOSED, awaiting user decision: one backend import adapter uses direct PDF text/layout extraction where available and local-to-deployment OCR for photos/screenshots/scanned pages, then format-aware parsing into the shared ImportDraft. Review, direct correction, omitted-activity addition and explicit confirmation are mandatory. Preserve uncertainty and unknown source codes; OCR confidence is not semantic truth. Layout-specific rules stay inside the adapter; generic PDF/JPG/PNG input does not promise universal layout interpretation and does not make Tide an integrated data source. Bound: import adapter, review UI and plan revisions. Prevents: treating extracted text as confirmed work or leaking document layout into the domain. Alternative considered: external document/vision service, with additional processor, connectivity and possible cost; not selected silently. Candidate tooling and representative-file qualification are implementation work, and an inadequate local pipeline must trigger an explicit revisit, not silent replacement by manual-only or demo import.
- (direction) AD-6 PROPOSED raw-source lifecycle: backend raw files and extraction intermediates exist only for bounded processing, are deleted when the draft result is ready or processing fails/is cancelled, with orphan cleanup after interrupted processing. The review UI references the user's selected original only transiently during the review session, never storing it in IndexedDB or Service Worker cache; release the reference at confirmation/cancellation/leaving review. Reopening after reload requires reselecting the original for source preview. Apply to own and accompanied-person uploads; the user-held original is outside app deletion. Retain only necessary interpreted draft/plan information and field provenance, not whole-source OCR transcripts or thumbnails as a substitute raw archive. Existing confirmed plan is unaffected until explicit scoped revision confirmation. This resolves source-side review versus prompt raw-file deletion as a proposal requiring user choice; do not describe browser memory disposal as guaranteed physical erasure. Exact temporary-job limits and abandoned-draft cleanup remain implementation/retention follow-up.
- (decision) AD-6 [ADOPTED] - Backend text extraction/OCR behind the import port, explicit review and transient raw-source handling. Binds: upload pipeline, ImportDraft, review UI, plan confirmation and temporary-file lifecycle. Prevents: unreviewed extraction changing the plan, raw files becoming a retained archive, and layout-specific parsing leaking into domain logic. Rule: extract available PDF text/layout; render every relevant scanned-PDF page to an image before OCR; process JPG/PNG through OCR in our backend deployment. Always allow field correction and require explicit confirmation before a plan changes. Qualify pdfplumber and Tesseract on representative anonymized shift files before locking tools; do not treat documentation as pilot-quality evidence. Backend raw files and processing copies are temporary, deleted when a draft is ready or processing fails/is cancelled, with cleanup of interrupted processes. Source preview uses only the transient browser-selected original; reselect it after reload. Apply to own and accompanied-person uploads. Rationale: one controlled import boundary, local/Docker operation, minimal source retention and consistent user review without a required external document/vision service.
- (constraint) AD-6 user clarification: ImportDraft with interpreted fields, uncertainty and user corrections must resume after page reload; only the original-file preview is lost. Make this distinction explicit in the UI. Persist draft edits transactionally in IndexedDB under AD-2, with necessary draft metadata/content in the existing minimal sync/recovery contract, not raw source bytes. Uploaded raw files and processing copies must never enter logs or persistent intermediate storage. Temporary processing must be bounded and cleanup cover cancellation, errors and interrupted processes. The original user-held file is outside app cleanup; no guarantee of physical erasure from browser memory is implied. Failed interpretation must still yield or preserve an editable draft for manual entry/correction; failure must not overwrite already entered corrections or confirmed plans. This manual recovery requirement does not remove V1's automatic extraction capability or its qualification gate.
- (question) AD-6 adopted; remaining import implementation details include exact anonymized qualification fixtures/acceptance checks, temporary processing limits and abandoned-draft expiry within the agreed retention policy. Next coaching decision AD-7 concerns external transit/notice acquisition and normalized snapshots linked to the confirmed plan, without live-feed dependence during driving or silent substitution of operational truth. Preserve Svipper-origin automatic-notice requirement; Entur transport and pilot coverage must be distinguished from proven feasibility. Detailed notice freshness/version/ending behavior and actual device/GPS qualification remain follow-up decisions as needed.
- (version) AD-7 documentation verified 2026-09-23; no live feed or representative-trip test performed. Current official Journey Planner v3 documentation describes the Transmodel GraphQL API, scheduled and realtime inputs and obligatory ET-Client-Name (https://developer.entur.no/docs/open-services/journey-planner). Point-to-point search returns selected Pareto-optimal journeys and may filter alternatives, so it cannot establish exhaustive imported-shift matching by itself. Official examples separate departure boards, planned/actual times, stop information and situations (https://developer.entur.no/docs/open-services/journey-planner/examples). Current rate policy requires respecting quota reset headers after quota violations (https://developer.entur.no/docs/open-services/journey-planner/rate-limiting). Older timetable export documentation remains linked from the migrating official portal and describes NeTEx/GTFS, provider-dependent completeness, nightly exports and expected at most daily download use (https://developer.entur.org/stops-and-timetable-data/).
- (version) AD-7 notice candidate verified against current official documentation 2026-09-23: https://developer.entur.no/open-data/realtime (updated 2026-08-31) lists TRO SIRI SX support but leaves GTFS-RT Service Alerts blank. SIRI 2.0 Lite GET /realtime/v1/rest/sx supports datasetId; documented limit four requests per minute. Default IDs are mapped to static route IDs, but actual correspondence must be tested. requestorId makes subsequent responses change-only and expires after a short session; MoreData marks incomplete pagination. Successful empty delta is not an empty complete set. Publication, mapping, coverage and ending semantics for actual Svipper messages remain unverified; do not claim a tested direct Svipper API or equivalent GTFS-RT coverage. Documentation is a source candidate, not pilot acceptance evidence.
- (direction) AD-7 PROPOSED, awaiting user: backend owns external transit/notice acquisition behind the already approved two ports and normalizes only V1 needs into source-versioned snapshots; clients use their prepared whole-day data and do not query Entur directly for operational progression. Prefer Entur Journey Planner v3 targeted departure/service-journey/stop lookups at preparation and confirmed plan changes, with backend reuse/cache and complete necessary stop patterns saved into OfflineBundle. Do not use passenger-optimal point-to-point suggestions as the complete candidate set. Alternative is periodic regional NeTEx/GTFS import into a local timetable catalogue, requiring extra import/calendar/version/ID-mapping work; revisit explicitly if API qualification fails rather than silently implementing both. Concrete API query/schema choices remain qualification/story detail. Use the ET-Client-Name identifier, source attribution and quota-aware acquisition. Existing prepared data survives source outages; new or unprepared data may need connectivity, and readiness reflects actual saved contents. Basis: AD-2 operational authority and bounded V1 implementation.
- (direction) AD-7 PROPOSED matching and update invariant: external transit data enriches confirmed plan activities and is not the plan or actual progress authority. Match external stable references when available or an evidence-based combination of provider/line, operating date, scheduled departure, direction/endpoints and stop pattern; line number alone and reporting time alone are insufficient. Distinguish full journey from an imported segment, scheduled from estimated/observed time and stop occurrences from reusable stop IDs. Preserve separate own/linked plan identity. Only unique adequately supported matches may be associated automatically with visible provenance; ambiguous candidates require choice, absent matches remain unknown with the accepted missing-stop fallback. Retain selected source version and user overrides. A new source result must not silently change the confirmed remaining plan, active trip/pin, performed evidence or manual corrections; a material remapping or stop-pattern change is proposed for explicit confirmation. Exact scoring/tolerances remain qualification/story work.
- (direction) AD-7 PROPOSED notice acquisition: initial V1 candidate Entur SIRI SX 2.0 Lite filtered by pilot-configured datasetId TRO, behind the notice port. Preserve origin/publisher separately from Entur transport; validate that retrieved notices actually cover Svipper-origin requirements. Central backend polling targets two-minute checks during active work with quota/error handling, plus preparation fetch; client receives normalized notice versions/source status and applies local relevance/movement rules. Retain last usable data on failure and distinguish failed/partial fetch, successful full empty set and empty delta; disappearances alone cannot establish ending. Preserve source update versus retrieval versus client receipt timestamps and uncertainty. Later AD-8 can settle remaining notice-version/relevance/freshness implementation invariants without reopening approved UX. Qualification before pilot acceptance must cover four pilot lines, direction/date/stop variants and calendars, complete-day preparation, source-ID mapping, actual Svipper publication/update/withdrawal cases, pagination/delta recovery and offline failure behavior. Neither endpoint availability nor documentation proves coverage. No silent substitution by manual/demo notices, scraping or operator access; inadequate candidates trigger an explicit decision.
- (decision) AD-7 [ADOPTED] - API-based whole-day preparation, persisted source data and evidence-based trip association. Binds: transit and notice adapters, preparation, OfflineBundle, source ingestion and client operational display. Prevents: live source dependence during a prepared day, line-number-only matching, silent replacement of confirmed/manual work and treating a candidate feed as proven coverage. Rule: use API-based preparation for the complete working day and persist necessary day data; unique adequately supported transit matches may associate automatically, ambiguous matches require a choice, and no match preserves known plan facts with missing-data status. Source updates cannot silently change confirmed plans/manual corrections/active selections. Entur Journey Planner v3 remains the targeted lookup implementation seed subject to qualification; no separate bulk timetable integration is authorized by this decision. User approves SIRI SX/TRO only as a candidate to test on actual pilot data, not verified complete Svipper coverage. Rationale: bounded V1 acquisition work with recoverable whole-day offline use and explicit evidence limits.
- (constraint) AD-7 user clarification: backend fetches/reuses data per source and configured pilot area, never once per active driver. Roughly two-minute polling is an initial target, subject to actual frequency, quota and source tests. Maintain distinctions among empty delta/no changes, confirmed absence of active notices within a verified complete source scope, and retrieval failure. Updated versions must supersede previous active display versions; confirmed ended notices must leave active warning display, preserving the authoritative UX ten-minute struck-through overview transition and summary history. Test actual frequency, ID mapping, coverage, updates and endings before pilot acceptance. Missing or uncertain feed coverage must remain visible.
- (constraint) AD-7 relevance evidence boundary [ADOPTED]: show only relevance supported by source references and the matched available transit data. Preserve provider/line/journey/stop references and the actual scope they establish; do not promote a line association or roadwork/diversion text to precise bus-road-path relevance. Precise road-segment or approaching-location claims require sufficient route/geometry evidence; V1 does not gain NVDB/DATEX/map matching or a computed diversion through this approval. Unknown association/coverage remains visibly uncertain; it must not be filtered away and presented as all clear. This inherits the prototype lessons recorded in UX; no prototype implementation/artifact was supplied or examined.
- (question) AD-7 adopted. Next AD-8 is the architecture of notice lifecycle: canonical source identity/version/ending in backend versus per-day local display/seen/acknowledged/manual-removal state, complete-versus-delta reconciliation and stale/unknown behavior. Do not re-ask established ten-minute ended display, silent updated-notice behavior or movement policy; preserve them while proposing technical consistency rules. No new device/source test has been performed.
- (version) AD-8 source semantics checked 2026-09-23 in the official Nordic SIRI-SX profile, current page dated 2024-12-02: https://entur.atlassian.net/wiki/spaces/PUBLIC/pages/637370605/SIRI-SX . It defines ParticipantRef/SituationNumber, optional Version and VersionedAtTime, Progress open/closed, and one or more ValidityPeriods. Closed records may omit summary/affected-reference content; a decoder must not reject a valid closure for lacking open-message fields. The validity example at https://entur.atlassian.net/wiki/spaces/PUBLIC/pages/637370694/SIRI-SX+-+Validity+of+a+message distinguishes a bounded open notice from a closure; closed records remain redistributed until a later EndTime, set at least five hours ahead for delivery. Profile semantics do not prove TRO behavior; fixture/live qualification is still required.
- (direction) AD-8 PROPOSED canonical notice and presentation separation, awaiting user: backend normalizes stable source-scoped identity, latest accepted version, source status/validity and evidence-backed applicability; the client owns workday-local seen/acknowledged/manually-hidden state keyed by notice identity/version, plus display and ten-minute end-registration timing. Preserve local user decisions across refresh/reload through transactional state/outbox. A valid newer revision supersedes the previous active revision without deleting historical displayed evidence; distinguish separate notices even if titles/lines coincide. An identical repeat or only changed transport/fetch metadata is not new content. Use trustworthy source revision ordering when supplied; keep a normalized content fingerprint for idempotent comparison where needed, without pretending a hash orders revisions. Older or conflicting evidence must not silently roll back newer/closed state; unresolvable order is a source-quality uncertainty to reconcile. Exact normalization and optional source-version mapping are adapter/story details to verify.
- (direction) AD-8 PROPOSED lifecycle: explicit source-confirmed closing removes the active driving warning/stop marker when safely applied locally, irrespective of a future delivery EndTime; keep the already agreed struck-through overview item for ten minutes from the client's first persisted registration of that ending, then summary-only under existing retention. Duplicate delivery or restart cannot reset the timer. Preserve enough prior message context when a valid close record has only identity/status. Validity windows are separately evaluated, including multiple future periods: outside the applicable window a notice is not an active current warning; an elapsed final source validity window is shown as expired validity, not proof that road conditions are normal or a source-confirmed closing. Disappearance from a complete feed without explicit closing remains uncertain under existing UX, not automatically resolved; empty delta leaves existing records intact. Previously recorded expiry does not turn a mere re-fetch of the same version into a new warning; an actual supported newer/reopened version follows changed-version behavior.
- (direction) AD-8 PROPOSED ingestion/freshness consistency: one shared acquisition state per source/configured area; explicitly track full snapshot versus delta, scope/completeness, successful source check versus partial/error response and last usable result. Apply validated positive updates/closures and their source evidence consistently; a partial result cannot establish absence or advance a complete-success marker. Commit applied updates with corresponding processing position so restart cannot lose a closure by skipping its consumed input. Cursor/session loss or restart requires documented baseline recovery before absence claims. Only a complete successful in-scope baseline plus consistent subsequent changes and no unresolved prior disappearance supports 'no active messages in the checked source scope'; never a universal all-clear. Preserve last usable data on errors with visible age/failure/coverage evidence. Source update, fetch/check and client receive times remain separate; receiving cached data does not make its source evidence fresh. Exact staleness thresholds/retry settings are source-configured qualification details, not one universal expiry that closes incidents.
- (constraint) AD-8 inherits already approved UX rather than reopening it: opening marks that version seen, optional Registrert dismisses only its prominent message, and neither means source ending or understanding. Acknowledgement does not remove a still-applicable stop marker or shift-detail entry. Changed applicable versions regain unseen emphasis silently; genuine new identity received during the current trip may chime only when current-trip relevance is supported. Poll duplicates, updates, source recovery replays and previously fetched messages merely becoming relevant cannot chime. Manual removal of disappeared/uncertain notice after source check hides unchanged content for the day and survives restart; changed versions return silently. Current movement policy governs all details/source/ack actions. AD-7 forbids precise road-path/stop warning claims without adequate source-reference and route evidence; uncertain regional notices must not become precise approaching-road warnings.
- (question) Present AD-8 for user acceptance: canonical versioned notices plus separate local presentation state, explicit close/validity-expiry/disappearance outcomes and completeness-aware source reconciliation. Show a compact outcome table and explain preserved ten-minute overview rule versus immediate active-warning removal. Qualification stories must include duplicate/newer/older revisions, sparse closing records, expiry/multiple intervals, full-empty versus delta-empty versus partial/error, outage exceeding closure distribution window, restart and local acknowledgements/manual hides. No notices were fetched live or pilot cases tested in this turn.
- (decision) AD-8 [ADOPTED] - Canonical source notice lifecycle separated from local version-specific presentation state. Binds: notice adapter, shared source store, client operational state, display, recovery and summary. Prevents: stale versions reviving ended notices, transport refresh clearing driver decisions, missing delta entries being treated as endings and validity expiry masquerading as confirmed road recovery. Rule: backend owns normalized source identity/version/status/validity and supported applicability; active client owns per-day seen/acknowledged/hidden state for the actual displayed version. Preserve the proposed complete-versus-delta, failure/completeness and ordering safeguards. Source-confirmed ending immediately removes active warning and active stop marker, then retains the agreed struck-through overview entry for ten minutes from first persisted client registration, without timer reset on retry/restart; summary history follows existing retention. Elapsed source validity and uncertain disappearance remain distinct from confirmed closing. Rationale: source updates and driver actions remain consistent across independent ingestion, presentation and recovery code.
- (constraint) AD-8 user refinement [ADOPTED]: read/seen, registered/acknowledged and hidden apply to the version the driver actually saw. A newer version with materially changed content or source-backed applicability/relevance returns as updated and unread even if the previous version was hidden; no sound for that update. Identical content or merely a new fetch timestamp must not reset those states. A notice becoming relevant through local trip/progress change alone is not a new source version. Retain source identity/version ordering evidence and normalized meaningful-content comparison; exact adapter field mapping is story detail. Absence from an incomplete response never closes an incident; an older source revision cannot reopen a closed one. Existing distinct rule permitting a genuinely new relevant notice's chime is not changed by this update-specific no-sound clarification.
- (question) AD-8 adopted. Next AD-9 is the local position/progression and movement-policy runtime: one coherent transition authority independent of rendering, quality-gated sensor evidence and recovery of manual pins/context/timers without schedule-only progression. Inherit accepted trip transitions, missing-stop behavior, 100-metre travel acceptance target and movement exceptions rather than re-deciding them. No sensor thresholds, device evidence or GPS accuracy claims have been approved merely by selecting a web client.
- (version) AD-9 browser evidence verified 2026-09-23: Geolocation watchPosition requires secure context and permission (https://developer.mozilla.org/en-US/docs/Web/API/Geolocation/watchPosition). speed is metres/second or null when unavailable (https://developer.mozilla.org/en-US/docs/Web/API/GeolocationCoordinates/speed); accuracy describes positional uncertainty in metres (https://developer.mozilla.org/en-US/docs/Web/API/GeolocationCoordinates/accuracy), not a supplied speed-accuracy guarantee. W3C Geolocation https://www.w3.org/TR/geolocation/ is source-agnostic, timestamp dates acquisition, and enableHighAccuracy is a hint without an accuracy guarantee; position acquisition may wait for a visible document. Documentation establishes API semantics, not exact Lenovo/Brave foreground/background/offline or speed performance. No target-device measurement or application implementation occurred.
- (direction) AD-9 PROPOSED, awaiting user: implement one client operational transition authority in ordinary framework-independent TypeScript, with explicit states/events and small internal functions for sensor qualification, progress, activity/context changes and interaction policy. Sensor readings, manual commands, confirmed plan changes and timer/lifecycle signals enter this authority; React renders its state and requests actions. Serialize accepted operational transitions through AD-2 IndexedDB state/outbox commit before publishing completion; do not persist every raw sensor sample or put business transitions in competing React effects. Persist minimum recovery/derived evidence; raw position samples use only a bounded transient working buffer and do not form a permanent track or backend stream. Share the pure rules with isolated fictional simulation, whose observations retain simulated origin; no state-machine framework, universal internal port layer or event-sourcing platform is selected. Binds progression, movement controls, own/linked context, persistence, summary and demo. Prevents independent widgets or timers deciding contradictory active trip, stop, authority or allowed actions.
- (direction) AD-9 PROPOSED qualification/progression: browser geolocation is a candidate observation, never automatically a reliable GPS fix. Qualify acquisition age, positional accuracy, plausible movement and consistency over observations; handle invalid/older samples explicitly. Assess position/progress suitability separately from speed/movement suitability; missing/stale speed stays unknown, never zero, and coordinate-derived speed cannot be trusted without independent qualification. Prefer sequence-aware recognition of actual arrival/departure/passage within the active dated trip using stop occurrences, direction and recent evidence. Alternative a single nearest-stop/radius rule is simpler but insufficient alone for shared stops, return direction, drift and skipped stops. Do not silently change trips from proximity/schedule; ambiguous initial candidates require choice. Unreliable position preserves last-confirmed progress labelled uncertain rather than current location. Reacquire at a supported later stop within the same active trip, including multiple-stop gaps, without fabricating intervening visits or diversion stops. Spatial entry/exit thresholds, freshness windows and stabilization rules are calibration/story detail; the inherited 100 metres travelled after passing/leaving is an acceptance target, never a default proximity radius.
- (direction) AD-9 PROPOSED authority/recovery: active tracking context includes own versus linked plan, person/block, actual driver/guiding role, manual trip pin and stop evidence; restore them consistently. A pin survives schedule/source refresh and sensor recovery within its context until actual completion, explicit correction/interruption or an approved context change. Context exit does not invent completion; acute FADDER takeover keeps context/pin and immediately applies driving restrictions. Position and speed loss, network loss, app suspension and source staleness remain distinct states. Recover last operational state but require new qualified sensor evidence rather than restoring old speed as current or treating restart as fresh startup. Persist first-valid-speed history and outage timing so startup/five-minute exceptions cannot reset by reload. Retain the existing movement table unchanged; a usable position with unavailable speed can support progress while interaction policy remains in unknown-speed state. Direct GPS-loss previous/next controls follow position-loss policy, not mere network loss. No assumption of continuous browser background tracking; on return expose unobserved intervals and reacquire without invented progress.
- (constraint) AD-9 inherited outcomes and qualification scope: ordinary final-stop Siste stopp display lasts ten seconds; same-route return uses its distinct reliable-position/manual-next trigger. Completion of non-passenger activities is not proven by display/timetable alone; own-day finalization remains explicit. Preserve adopted stop-related warning timing and evidence limits. Validate actual Lenovo/Brave for permission/HTTPS, stale/missing/null-speed readings, drift while stopped, passed stops, shared/opposite-direction/repeated stops, recovery several stops ahead, delay/manual pin, browser suspension/restart and hotspot loss with usable location. Test role/context transitions and timer races in deterministic scenarios, then actual-device behavior; simulation cannot establish pilot GPS acceptance. Unsupported capability or failure to meet 100-metre travel target triggers an explicit implementation/architecture revisit rather than fabricated confidence, a silent relaxed threshold or substitution by timetable-only operation.
- (question) Next user choice is AD-9: accept one UI-independent, locally persisted operational state machine with separately qualified position/speed evidence and sequence-aware progression, while keeping numerical thresholds and optional library choice for device-backed implementation qualification? Present the single-radius alternative and the cost of extra state/qualification/testing; do not ask the user to reconfirm the already settled movement/transition UX. Subsequent interactive decisions still include authentication/offline access, explicit writer takeover/conflict resolution, lifecycle/deletion and deployment/operations.
- (decision) AD-9 [ADOPTED] - One React-independent operational state machine in TypeScript using stop sequence and qualified observations. Binds: sensor qualification, stop/trip progression, movement controls, tracking context, local persistence/recovery and simulation. Prevents: independent UI components making conflicting operational transitions, proximity alone proving passage, missing speed becoming zero and stale recovered measurements becoming fresh. Rule: qualify position and speed separately, use the active stop sequence and supported movement evidence, retain manual choices as authoritative within their approved context, and recover saved state without reclassifying old GPS data as current. Do not store a permanent driving track. Determine numerical thresholds through actual Lenovo/Brave testing rather than inventing values in the architecture. Rationale: consistent local operation and explicit uncertainty across network and sensor interruptions.
- (constraint) AD-9 user clarification [ADOPTED]: the target of progression within 100 metres travelled after actual stop passage/departure requires field verification; it is never a radius that proves passage. If browser/device accuracy or update frequency is insufficient, visibly retain uncertain progression and offer manual correction under the established movement policy. Do not claim automatic stop-following meets the target without evidence. Missing speed remains unknown, never 0 km/h. The previously approved five-minute unlocking exception must not depend on accelerometer-based speed measurement; no accelerometer integration or inferred speed is a prerequisite for that timer or its expiry.
- (question) AD-9 adopted. Next AD-10 concerns authentication/session and offline authorization: private remembered sign-in for 14 days, active-day continuity after normal expiry, explicit logout/revocation, backend access enforcement and isolation of the public fictional demo. Need a concrete proposal for session mechanism and distinction between general signed-in access and continuation of a previously prepared/authorized day; do not silently copy template JWT/localStorage defaults. Offline revocation cannot be observed immediately, and logout must not silently destroy pending work. Writer handoff/conflict resolution and exact cleanup/deployment remain later decisions.
- (version) AD-10 authentication mechanism researched 2026-09-23 in official sources: OWASP Session Management https://cheatsheetseries.owasp.org/cheatsheets/Session_Management_Cheat_Sheet.html covers opaque server sessions, secure/HttpOnly cookies, server-side expiry and logout invalidation. CSRF guidance https://cheatsheetseries.owasp.org/cheatsheets/Cross-Site_Request_Forgery_Prevention_Cheat_Sheet.html supports request-origin/token protections in addition to SameSite. FastAPI supports setting response cookies (https://fastapi.tiangolo.com/advanced/response-cookies/); this does not constitute a complete supplied auth implementation. Cookie host restrictions are documented at https://developer.mozilla.org/en-US/docs/Web/HTTP/Reference/Headers/Set-Cookie . OWASP HTML5 Security https://cheatsheetseries.owasp.org/cheatsheets/HTML5_Security_Cheat_Sheet.html warns that IndexedDB is not a confidentiality boundary against browser-profile access; do not describe a local app lock as encrypted storage. No auth package or new dependency selected.
- (direction) AD-10 PROPOSED, awaiting user: provision the private pilot account administratively, with password authentication and established password hashing, no public registration/email-recovery product. Use an opaque random session identifier in a host-only Secure HttpOnly SameSite cookie with backend session records in PostgreSQL; frontend/private API share an origin. Enforce ownership and scope on every private request; apply CSRF protection to state-changing requests, including login/logout, and rate-limit login. Session credentials must not be placed in localStorage/IndexedDB or logs. Real alternative is access/refresh bearer tokens as in the stock template; it adds token renewal and revocation coordination without a V1 need for multiple API consumers. Prefer server sessions for this one browser/backend application. Exact library, password-hash parameters and implementation fields remain story details; do not blindly adopt signed client-side session middleware as a substitute for revocable backend state.
- (direction) AD-10 PROPOSED remembered access versus workday continuation: ordinary remembered access lasts fourteen days from successful sign-in/reauthentication and is not silently extended by polling. During authorized preparation backend records a bounded continuation permission for a specific workday and client/writer context. Normal expiry cannot interrupt an already active prepared day, offline restart, local summary/export or the online imports/source refresh/sync necessary for that same day. After normal expiry it does not grant a new day, account changes or access to other days. A new day after expiry requires online reauthentication. Scope and revocation remain backend-validated; a client active=true flag or claimed occurrence time cannot mint server permission. Provide continuation credentials through server-controlled cookie/session state, not a broadly reusable script-readable bearer token. Cookie transport lifetime must cover the pre-authorized day allowance even when general access expires. The day allowance has an issuance-time bounded expiry no later than applicable retention expiry, cannot be extended merely by polling/export/clock changes, and narrows after end/abort to permitted final-summary work and outstanding idempotent synchronization. It never permits resumed ended work. Precise grant schema/endpoints are implementation detail subject to these invariants.
- (direction) AD-10 PROPOSED offline/local access and explicit logout: a prepared day stores only necessary local authorization/recovery metadata alongside its data, not a password or backend credential. With ordinary session expiry, already active work continues under the approved bounded day permission; server calls still require valid server-recognized scope. Explicit logout immediately invalidates local continuation, stops private use/automatic sync and locks the private application; renewed access requires online authentication. Pending work is not silently deleted by logout or an auth error and remains unavailable to other accounts/demo; recovery requires reauthentication as the same authorized owner and later AD-11 writer/conflict handling. Online logout revokes the server session as well. Offline logout records pending server revocation, processed before other private traffic on reconnect, and must not claim remote session revocation completed while disconnected. No blanket clear-site-data/delete-outbox logout that destroys unsynchronized evidence. Existing retention still governs retained pending data.
- (direction) AD-10 PROPOSED revocation and isolation: backend rejects revoked session/account/day permission immediately for subsequent requests; a connected client applies confirmed revocation by locking private access and stopping private sync. An offline client cannot know a remote revocation until communication resumes; do not promise immediate offline revocation or remote erasure. Keep pending work protected by application access checks until an authorized resolution or expiry. Offline browser storage assumes a controlled pilot device/browser profile with OS screen lock; no extra offline PIN/custom local encryption system is proposed, and app-level locking is not protection against someone who controls the browser profile. Public no-login fictional demo uses a separate browser origin/host and storage, contains only fictional fixtures and no private API credentials/access. Reuse frontend/rule code; this does not require a second backend monolith or database. Use host-only operational cookies and do not enable credentialed demo-origin access to private APIs; paths, database names or ports alone are not claimed to isolate all credentials. Concrete local/Docker hostnames/TLS routing belong to deployment.
- (question) Present AD-10 for choice with concrete session versus bearer-token comparison and a table covering fourteen-day expiry during active work, a new day after expiry, explicit offline logout and remotely revoked access. Explain the material offline-revocation/device-access limit without implying local cached metadata authenticates backend requests. The proposal also includes isolated public demo origin and provisioned pilot account, not a multi-account administration product. User has not yet approved AD-10. Subsequent decisions still include controlling-client transfer/conflict resolution, derived-data expiry and deployment/operations.
- (decision) AD-10 [ADOPTED with requested clarification] - Provisioned private pilot account, revocable server sessions, bounded day continuation and isolated fictional demo. Binds: login/session service, private API authorization, local offline access, logout/recovery and deployment origins. Prevents: normal 14-day expiry interrupting an authorized active day, day continuation granting a new day, logout leaving known authorization active, and demo accessing private state. Rule: use a random opaque session identifier in a host-only Secure HttpOnly cookie with explicit SameSite=Lax, server-side session/grant records, and CSRF protection for state-changing requests (including login/logout). General access lasts 14 days from authentication without renewal by polling. Day continuation is explicitly limited to a concrete previously authorized workday and client context; server enforces ownership, scope, fixed expiry and revocation. No public registration. Public demo runs under a separate origin/host with fictional data only and no private-backend or private-session access. Rationale: one revocable authentication mechanism for the browser/backend, with offline continuity and isolation matching the accepted V1.
- (decision) AD-10 deadline/credential clarification fixed under the user's request to make interaction concrete: general_until = authenticated_at + 14 days. At authorized day-grant issuance, fix day_grant_until no later than the then-confirmed planned_final_end + 7 days, using the already approved retention horizon as the maximum rather than adding another retention period. This hard grant deadline is not slid by polling, export, retries or local clock edits. After end/abort, allow only permitted final-summary operations and idempotent settlement of the same day until settlement_until = min(day_grant_until, ended_at + 7 days, applicable deletion deadline). Ordinary operation of an ended day is forbidden even before these deadlines. A late message cannot recreate expired/deleted work. The cookie/session identifier remains usable for scoped identification until the latest still-valid server-authorized scope deadline: do not expire/delete the cookie or whole session record at general_until while the day grant is valid. Cookie lifetime does not grant general authority; after general_until every request must fit the specific unrevoked day scope and its deadline. Prepared-but-not-started days cannot use this exception to start a new day after general expiry. Exact table layout and grant DTOs remain story detail; genuine scope/deadline extension requires explicit backend authorization under renewed authentication.
- (constraint) AD-10 user logout clarification [ADOPTED]: explicit logout immediately and durably locks local private use/continuation. Preserve unsynchronized events under existing deletion deadlines; only the same user after fresh online login can recover them, subject to writer/conflict resolution. While offline show that server logout is pending; do not claim immediate server revocation. When the application actually runs with connectivity, attempt server revocation before other private traffic, including reopening/sync of the old day. When received, logout/revocation atomically invalidates both the relevant server session authority and its day continuation grants; account revocation invalidates all affected grants. A known revocation cannot be bypassed by selecting offline mode. No guarantee of closed-browser background execution, instant remote offline revocation or browser-profile encryption is implied.
- (question) AD-10 registered with concrete independent authority deadlines and surviving scoped session identification. Next AD-11: one controlling client/tab, explicit online handover and exceptional takeover, preservation/review of unsynchronized branches and bounded conflict resolution without generic automatic merge. Must integrate existing writer_epoch/expected_revision/idempotency with day grants and prevent a timeout/network gap automatically transferring ownership. User requested interactive decision points, so present the proposed control/recovery policy before adopting it.
- (version) AD-11 mechanism evidence checked 2026-09-23: Web Locks coordinates tabs/workers within the same origin and requires a secure context (https://developer.mozilla.org/en-US/docs/Web/API/Web_Locks_API); it does not coordinate separate devices and is not an account authorization boundary. PostgreSQL 18 supports row-level locking within transactions (https://www.postgresql.org/docs/18/explicit-locking.html), enabling atomic ownership/revision checks. Use short control/sync transactions and stored writer assignment; never hold a database transaction open for an entire workday. Documentation is not a Lenovo/Brave concurrency/recovery test. Web Locks is an implementation seed for tab coordination, subject to actual target testing.
- (direction) AD-11 PROPOSED, awaiting user: retain one controlling browser installation (client_id) and one mutating tab per active day. Coordinate tabs locally, with Web Locks as a qualified implementation seed and all mutation entry points honoring current local authority. Backend stores controlling client and monotonically increasing writer_epoch; new writes require authenticated owner/day scope, current epoch and expected operational revision in the same short transaction. A timeout, missing heartbeat, session renewal or temporary disconnect never transfers day ownership automatically. Alternative short leases with automatic takeover would improve unattended availability but can grant a second writer while the first works offline; prefer explicit transfer. A normal restart of the same valid client restores its saved epoch/state; it does not silently claim another generation.
- (direction) AD-11 PROPOSED planned handover: explicit online operation under permitted interaction policy. Old client first stops accepting new operational mutations for handover, durably preserves local work, drains pending synchronization and supplies only the minimal coherent recovery checkpoint required with acknowledged events/confirmed plan versions. New client fetches that server-confirmed state and required day data, verifies local readiness, and explicitly requests control. Backend atomically checks expected current revision/epoch and transfers writer identity to a new epoch with the appropriate scoped day permission; handover retries are idempotent. New client becomes operational only after confirmed transfer and durable local recovery state. In-flight old writes serialize with transfer: accepted-before-transfer work is reflected in its revision; old-epoch writes after transfer are rejected. Interrupted transfer stays visibly pending, with state inquiry/idempotent retry rather than two assumed writers. No full OperationalState object on every edit and no duplicate Python GPS/runtime logic is introduced.
- (direction) AD-11 PROPOSED exceptional takeover and conflict detection: if the old client is unavailable, the same authenticated owner may explicitly take over online after seeing the last server-confirmed state and the risk that unsent work is absent. Recover only acknowledged events/checkpoints; never claim recovery of inaccessible device-local data. Old offline client cannot be physically stopped, but once it learns its writer_epoch is superseded it ceases operational writes/automatic retries and preserves pending work as a non-current branch for explicit review. Revision conflict with unchanged ownership pauses sync, not the authorized local operational runtime; pending work can accumulate with clear status until permitted resolution. Keep backend revision-conflict, writer-conflict and auth-revocation outcomes distinct. An identical already-committed retry may still return its prior authorized receipt under AD-5 without granting the old writer new authority.
- (direction) AD-11 PROPOSED bounded conflict resolution: while interaction rules permit, show last synchronized server state and preserved unsynchronized local changes, and require an explicit choice of continuing operative state plus any manual corrections to carry forward. No automatic last-write-wins, timestamp precedence, generic merge or wholesale replacement of performed evidence. Selected corrections are new idempotent resolution events against the current authorized epoch/revision, referencing original provenance as needed; rejected immutable batches are not mutated and resent under the same idempotency key. Retain the original pending branch until its disposition is explicitly confirmed and accepted where server submission is involved; do not silently drop unselected events or call them synchronized. Record discarded/not-applied versus applied disposition under the existing day retention. Choosing local ongoing state still requires legitimate server writer transfer if another client owns the day. Ended days cannot be resumed and expired/deleted days cannot be recreated through conflict resolution. Full recovery payload, control endpoint inventory and comparison-screen layout are story details within these invariants.
- (question) Next user choice AD-11: explicit online client transfer instead of timeout-based takeover; normal synchronized handover and clearly marked exceptional recovery from last received server state; one local writing tab; conflicting unsynchronized work preserved for explicit correction/disposition. Qualification includes two tabs, old offline/new online client, concurrent transfer requests, old batch versus transfer race, lost transfer/sync receipt, restart mid-transfer, same-owner revision conflict and revoked/expired day. These are future implementation tests, not completed checks. After AD-11, continue with lifecycle/deletion and concrete local/Docker deployment/operations.
- (decision) AD-11 [ADOPTED] - Explicit online client handover, exceptional takeover and deliberate conflict resolution. Binds: local tab control, server writer assignment, recovery checkpoints, sync and conflict UI. Prevents: timeout-based duplicate writers, stale snapshots becoming operational after transfer and silent loss/merge of divergent local work. Rule: one controlling client and one mutating tab per active day; Web Locks coordinates only the same origin, while backend authenticates and enforces client_id/writer_epoch/revision across devices. Explicit normal handover synchronizes the old client; explicit exceptional takeover uses the last server-confirmed state with visible recovery limits. Preserve unsynchronized branches for review and resolve with explicit corrections/disposition under AD-5/AD-10, never silent replacement or automatic timestamp-based merge. Rationale: offline continuity makes timeouts insufficient evidence of relinquished control.
- (constraint) AD-11 user correction [ADOPTED] supersedes any earlier suggestion that a pre-transfer download alone is sufficient: old client first stops new mutations and synchronizes. Backend then atomically increments writer_epoch and returns the authoritative server revision at transfer. Before controlling the day, the new client must fetch or verify its recovery state and required day data against that returned revision and associated plan/bundle versions. If the old client committed more work after an earlier download, that older snapshot must not be used uncritically. Operational activation requires successful authority transfer plus revision-coherent local readiness; interrupted/lost replies use idempotent status/retry handling, not assumed ownership.
- (constraint) AD-11 exceptional takeover details [ADOPTED]: show the timestamp and revision of last work actually received by backend, separately from client-reported occurrence time, and explicitly state that unsynchronized events on the unavailable old device may be missing. When the old device learns of takeover it keeps those events locally for review, stops acting as controlling client and does not automatically synchronize them. Web Locks is only same-origin tab coordination; backend enforcement remains mandatory between devices. Retention, revocation and non-resumption of ended days still apply.
- (question) AD-11 adopted. Next AD-12 concerns derived-data retention and enforceable deletion across backend, client/drafts/outbox/conflict copies/logs and backups. Inherit seven days after actual confirmed combined-day end/abort, or planned final end when never ended; raw sources remain short-lived under AD-6; accompanied-only evidence survives own-day end. Need explicit treatment of closed/offline clients, abandoned draft expiry, deletion races/no resurrection and operational backup tradeoff. Preserve constraints rather than silently extending retention for safety copies.
- (version) AD-12 storage/lifecycle evidence checked 2026-09-23: Service Workers have event-bounded execution and may be terminated when idle (https://w3c.github.io/ServiceWorker/#service-worker-lifetime); scheduled erasure cannot be guaranteed in a closed/off device browser. PostgreSQL DELETE leaves old row versions until vacuum reclamation (https://www.postgresql.org/docs/18/routine-vacuuming.html), so application deletion must not be described as verified physical-sector erasure. Docker volumes persist across container replacement (https://docs.docker.com/engine/storage/volumes/); stopping/rebuilding containers does not delete database contents. Docker tmpfs is temporary but can spill to swap (https://docs.docker.com/engine/storage/tmpfs/); AD-6's nonpersistent raw processing therefore requires deployment verification of temp directories, swap/core dumps and tool output, not a tmpfs label alone. These checks do not establish legal compliance or a tested deletion implementation.
- (direction) AD-12 PROPOSED lifecycle ownership, awaiting user: give each workday-associated record a deterministic deletion scope and an explicit expires_at using the approved rule: confirmed actual combined-day end/abort plus seven days, otherwise confirmed planned final end plus seven days. Preserve the end occurrence/deadline through delayed sync; receipt time must not create a new retention window. Only a confirmed plan revision can change planned final end for a never-ended day; no poll, export, restart, reauthentication, handover or conflict resolution extends retention. Apply the deadline to plans/revisions, operational evidence, own/linked contexts, notices linked to that day, seen/ack/hidden states, summaries, recovery checkpoints, outbox, rejected/conflict branches and associated receipts/grants. No terminal history or pending-work exception beyond expiry. Account credentials/settings and bounded shared public-source cache have separate lifecycles and must not retain hidden day/person associations.
- (direction) AD-12 PROPOSED draft and linked-data minimization: unconfirmed ImportDraft remains reload-recoverable under AD-6 but has a fixed maximum seven-day lifetime from creation, shortened to an associated day deadline if earlier; editing/reopening does not slide it. Confirmation transfers necessary interpreted values, uncertainty, unexplained codes and correction provenance into the confirmed plan, then removes redundant draft copies. Cancellation/discard removes the draft when explicitly chosen; raw-source lifecycle stays immediate/transient under AD-6 and is not covered by this draft window. At final own-day end keep only actually accompanied evidence needed for summary and remove the other person's unaccompanied context, including redundant historical revisions/checkpoints/pending payload copies. Perform this locally at closing and on backend when the closing is received; disconnected stores cannot learn it instantly. Idempotency receipts store minimal identifiers/hash/outcome rather than a retained full private request body. Cleanup must reconcile pending submission state without changing a submitted batch under the same idempotency key; exact pruning/control flow is a required implementation story, not permission to retain forbidden copies.
- (direction) AD-12 PROPOSED enforcement: access, export, mutation and sync paths check expiry independently of periodic cleanup. Backend runs idempotent cleanup at startup and periodically, deleting related data in a workday-scoped transaction and refusing late sync that could recreate a deleted/expired day. Client runs the same deadline policy at startup/resume and while active, before private data display/export or outbox sending; clean IndexedDB plus any owned temporary export artifacts and remove expired conflict copies. Private API responses are not incidentally cached in HTTP/Service Worker caches; store approved offline data explicitly in IndexedDB and keep the Service Worker cache for the application shell. A closed/off browser cannot run exact-time deletion: overdue records are inaccessible through app recovery and are deleted at first execution before use, including offline startup. A disconnected client uses its last known deadline until it can learn an earlier remotely established end/deadline; no reconnect-created grace period. Clock anomalies cannot reset stored deadlines. Do not claim exact remote or forensic erasure. App/API expiry is immediate when executing; cleanup failures remain retryable/observable and do not restore access.
- (direction) AD-12 PROPOSED backup choice: for V1 use the persistent PostgreSQL working volume but no historical backups/snapshots/WAL archives of private shift data. Preserve reproducible code, migrations and fictional configuration/fixtures separately; user-exported private PDFs remain outside app cleanup as already agreed. This minimizes retained copies and avoids adding a backup-specific extension to the seven-day rule, at the explicit cost that loss/corruption of the backend volume may lose server history and cannot be promised recoverable from the client. The local client can continue an already prepared day but is not a complete backend disaster-recovery backup. Alternative is backups with enforced per-data expiry, deletion propagation and restore filtering; that requires an explicit V1 scope/operations decision. Do not disable normal PostgreSQL WAL or autovacuum; their low-level remnants are not historical product backups or a physical-erasure guarantee. Future backup introduction is a revisit trigger, never a silent deployment default or pilot archive.
- (direction) AD-12 PROPOSED privacy of diagnostics and verification: logs must omit raw uploads, OCR transcripts, private request/response bodies, credentials/tokens and identifying shift/person/GPS details; use route templates/status/error categories so URL path identifiers do not silently create a second history. Real operational data must not enter source control, container images, CI artifacts or development database copies. Existing explicitly authorized anonymized import qualification examples and fictional tests are distinct from a permanent operational quality archive. Required tests cover actual/planned/early/split-day end, late offline ending, abandoned/corrected draft reload, accompanied-only summary trimming across copies, outbox/conflict expiry, closed-browser reopen, deletion versus concurrent sync, cleanup restart/failure and grant/receipt purge without resurrection. Actual clock/browser deletion limits must be documented honestly. No implementation or deletion tests were run in this architecture turn.
- (question) Present AD-12 as a proposal: common explicit deadlines across copies, seven-day non-sliding maximum for unconfirmed drafts, cleanup/access guards with honest closed-browser/offline limitations, and no historical private-shift backups in V1. Reiterate existing main seven-day and accompanied-only outcomes only as inherited constraints, not questions to reopen. Ask user whether to accept the backup/recovery tradeoff or require backup scope now. After adoption, proceed to local/Docker deployment and operational availability.
- (decision) AD-12 [ADOPTED] - Common explicit data lifetimes and deletion across all workday copies. Binds: imports, plans/revisions, local runtime, synchronization/recovery/conflicts, summaries, access/grants, storage and operational diagnostics. Prevents: indefinite drafts, hidden retained private history, late-sync resurrection and backup copies silently extending retention. Rule: apply the proposed AD-12 lifecycle/enforcement rules: actual confirmed combined-day end/abort plus seven days, or confirmed planned final end plus seven days if never ended; fixed maximum seven days from creation for unconfirmed ImportDraft, shortened by an earlier associated-day deadline. Reopening, edits to drafts, syncing, exporting, handover and conflicts do not renew retention. Delete redundant drafts on confirmation/discard and unaccompanied linked-person context at own-day ending. Retain AD-6 raw-file deletion rules. Rationale: a single deterministic scope makes deletion enforceable across offline, server and conflict copies without introducing a retention exception for pending work.
- (constraint) AD-12 explicit user additions [ADOPTED]: show deletion deadlines in the interface, especially for unconfirmed import drafts and unsynchronized work; clearly distinguish retained-until from synchronization success so pending changes are not presented as indefinitely safe. Deletion includes private facts in older revisions, checkpoints and conflict copies, not just the current plan/view. A client returning from offline must not resubmit or recreate an expired workday; local expiry checks and backend access/sync guards both apply. Visible deadline/status must follow the approved motion/interaction restrictions rather than require unsafe interaction. Exact screen wording/layout and warning timing are UX/story details, but deadline visibility and honest pending-loss status are mandatory.
- (decision) AD-12 backup tradeoff [ADOPTED]: V1 keeps a persistent PostgreSQL working volume but no historical backups/snapshots/WAL archives of private shift data. Record possible permanent data loss from volume loss/corruption as a deliberate pilot limitation; client storage is neither guaranteed durable nor a complete server backup. Code/migrations/fictional fixtures remain reproducible. This does not disable normal database WAL/autovacuum or claim verified physical erasure. Backend guards block expired data independently of cleanup; browser cleanup occurs on execution, with the approved closed-browser/offline limitations. Introducing backups later requires an explicit retention/deletion/restoration design decision.
- (question) AD-12 approved. Next AD-13 must make local/Docker delivery and the operational connectivity envelope concrete: same-origin private web/API over trusted HTTPS on the tablet; database kept private; separate fictional demo origin; local development versus actual mobile pilot backend reachability; persistent working data without historical backups; one modular backend with bounded polling/cleanup/import execution; controlled releases/schema and Service Worker updates during active offline days. Asked the user which machine can run Docker during the pilot and whether it is reachable from the tablet over mobile data. Do not assume an internet-hosted service, provider, subscription or external publication is authorized. Continue independent topology and deployment research while waiting.
- (version) AD-13 initial deployment evidence checked 2026-09-23: Docker Compose documents repeatable development/test environments and single-host deployment (https://docs.docker.com/compose/intro/features-uses/). MDN secure contexts distinguish trusted HTTPS and local loopback origins from ordinary network HTTP; the tablet accessing a PC by LAN address is not accessing its own localhost (https://developer.mozilla.org/en-US/docs/Web/Security/Defenses/Secure_Contexts). This matters for Service Workers and the existing secure-cookie/GPS requirements. FastAPI supports one or multiple server processes (https://fastapi.tiangolo.com/deployment/server-workers/); process count is a deployment choice, not an inherent monolith requirement. Service Worker skipWaiting can immediately activate a waiting version across existing clients (https://developer.mozilla.org/en-US/docs/Web/API/ServiceWorkerGlobalScope/skipWaiting), so uncontrolled activation is incompatible with assuming a fixed active-day client version. Exact Docker/proxy runtime versions and hosting route are not selected by these documentation checks; no server, certificate, device or deployment test was performed.
- (direction) AD-13 initial PROPOSAL, not adopted: reproducible Docker Compose delivery on one host with a private HTTPS web entry serving the built React client and forwarding same-origin /api requests to one modular FastAPI backend, with PostgreSQL 18 reachable only on the internal service network. The fictional demo remains a distinct hostname/origin and build/configuration without private API credentials/routes; code reuse is allowed. Separate development/test/pilot data and secrets, use PostgreSQL for integration tests, retain only the working database volume under AD-12. Compose makes the package portable but does not by itself make a home PC reachable over mobile data. Operational live source refresh needs reachable running backend; otherwise show offline/stale behavior without downgrading the accepted automatic-source requirement. Final hosting/HTTPS path depends on the pending user environment answer; no vendor/service/purchase/public exposure is selected.
- (decision) AD-13 hosting envelope [ADOPTED by user]: the only current host is the user's private stationary PC. Use it for local development and tablet testing. Package the same application topology with Docker Compose so it can later run on a network-accessible host using environment-specific configuration rather than application rewrites. No server/provider has been selected or paid for. A host reachable from the tablet over mobile data with browser-trusted HTTPS is a prerequisite for actual-shift pilot operation; select hosting and cost near the pilot. Until then, a prepared day can be tested offline but new disruptions during a shift must not be assumed. This deferral does not remove the accepted automatic operational-source requirement. Binds deployment instructions and pilot readiness; prevents a local-only demo being treated as evidence of mobile operational availability. Remaining AD-13 deployment/runtime/update details are still proposals, not adopted.
- (decision) AD-13 hosting correction [ADOPTED by user, supersedes the prior local-only staging assumption]: consider the existing private desktop as the FIRST actual pilot host, not merely development/testing, provided Docker Compose has moderate resource use and does not cause objectionable fan noise. User can leave the PC running during shifts. Investigate secure tablet access over mobile data with browser-trusted HTTPS and no database exposure, and document restart prerequisites. Retain a prepared day's complete offline behavior during home connectivity or PC failure. A paid server remains a later fallback if home hosting proves impractical. This authorizes architecture research/read-only environment assessment, not installing a tunnel/VPN, changing firewall/startup/power settings, publishing a service, purchasing a server or provisioning an external account.
- (constraint) AD-13 read-only environment check 2026-09-23: docker.exe and wsl.exe are present on PATH; tailscale.exe was not found on PATH, which does not prove absence of every installation. Initial CIM queries were denied by the sandbox; the same read-only OS/CPU/memory inquiry was rerun with escalation and succeeded. The displayed result identifies Microsoft Windows 11 Home, version 10.0.26200, 64-bit. Mixed-object table formatting did not expose the CPU/RAM values, so no hardware capacity/noise claim is established. Docker executable presence does not establish a running/healthy Docker daemon. No services, networking, containers or startup settings were changed.
- (version) AD-13 external access research 2026-09-23: Tailscale Serve exposes a local service only inside the tailnet, applies network access policy and provisions HTTPS for the device tailnet DNS name (https://tailscale.com/docs/features/tailscale-serve; https://tailscale.com/docs/how-to/set-up-https-certificates). Serve is distinct from public Funnel; do not enable Funnel for the private app. Tailscale normally connects through NAT without manual inbound port forwarding and can fall back to relays (https://tailscale.com/docs/reference/faq/firewall-ports). Android requires the Tailscale app and VPN configuration (https://tailscale.com/docs/install/android), so this is an additional pilot infrastructure dependency even though the product remains a browser app. Current stable client release found is 1.102.4 dated 2026-09-10 (https://tailscale.com/changelog); recheck/pin supported versions during setup. Personal pricing currently offers a free non-commercial plan (https://tailscale.com/pricing); confirm pilot eligibility rather than assume commercial workplace use is free. HTTPS certificate transparency publishes the chosen host/tailnet DNS name; choose a neutral name before enrollment/certificate issuance. No account or network was provisioned.
- (version) AD-13 comparison evidence 2026-09-23: Cloudflare Tunnel supports a published browser-accessible HTTPS hostname forwarded to a local service and requires a domain on Cloudflare for this published-app setup (https://developers.cloudflare.com/tunnel/get-started/; https://developers.cloudflare.com/tunnel/concepts/routing/). cloudflared can run as a Windows system service (https://developers.cloudflare.com/tunnel/features/locally-managed-tunnels/as-a-service/). It avoids a tablet VPN client but adds a public ingress and an additional edge intermediary for private web traffic, which require explicit access/cache/privacy configuration. A random temporary Quick Tunnel URL is not an adequate stable origin for the private offline app. Prefer Tailscale Serve for the one-user private pilot if the user accepts installing its network client; preserve Cloudflare/public hosting as an alternative, not a simultaneously implemented second path.
- (version) AD-13 restart evidence 2026-09-23: Windows Tailscale supports Run Unattended (https://tailscale.com/docs/how-to/run-unattended) and tailscale serve --bg persists across reboot/Tailscale restart (https://tailscale.com/docs/reference/tailscale-cli/serve). Docker Desktop's documented startup option is after Windows sign-in (https://docs.docker.com/desktop/settings-and-maintenance/settings/), and the privileged com.docker.service is not equivalent to an automatically running WSL2 Linux application daemon (https://docs.docker.com/desktop/setup/install/windows-permission-requirements/). Docker restart unless-stopped restarts eligible existing containers after daemon restart but respects explicit manual stops (https://docs.docker.com/engine/containers/start-containers-automatically/). Compose health-based dependencies help compose-up ordering, while runtime database reconnect/backoff remains necessary on daemon-level restart (https://docs.docker.com/compose/how-tos/startup-order/). Healthcheck failure alone must not be described as an automatic container restart. No reboot/daemon recovery test has been run.
- (constraint) AD-13 environment inquiry completed with readable output: Windows 11 Home, Intel Core i7-8086K, six cores/twelve logical processors, approximately 32 GB RAM, hypervisor present. Docker CLI 29.7.2 exists and uses desktop-linux context when outside the sandbox, but the Docker Desktop Linux Engine pipe was unavailable; a running Docker daemon was not confirmed. Initial Docker read was blocked from its config by the sandbox; escalated read only retrieved version/status, no credentials were printed. Hardware is a plausible pilot starting point, not evidence of measured CPU/RAM use, energy consumption or fan noise. No Docker startup or configuration change was attempted.
- (direction) AD-13 revised PROPOSAL awaiting user: desktop-first single-host Docker Compose with built static web/API entry, one modular FastAPI application and private PostgreSQL18 working volume. For the first pilot use native Windows Tailscale plus Serve HTTPS to a loopback-only published web port (for example 127.0.0.1:8080), with /api forwarded internally; publish neither PostgreSQL nor the backend API port to LAN/internet/tailnet. Restrict network grants to the necessary pilot devices and HTTPS entry, no subnet router/exit-node requirement; keep AD-10 app login/session/CSRF unchanged and trust forwarded headers only from the configured proxy path. The tablet uses Tailscale over phone tethering; DNS/certificate/device authorization expiry and reconnection are checked before shifts. Use a stable private origin for cached days. The fictional demo remains a separate hostname/origin/config without private routes or credentials. Host migration changes browser origin unless deliberately preserved, so never imply its IndexedDB/session transfers automatically. Keep images/config portable for later paid hosting.
- (direction) AD-13 revised PROPOSAL runtime and quiet operation: one backend API process with centrally owned supervised source polling/cleanup; one bounded OCR job at a time in a child process so expensive extraction cannot block API/sync. Bound upload size/page count/rendering work, memory and execution duration; interruption keeps the recoverable interpreted draft/manual fallback and raw-file cleanup rules from AD-6. No GPU, continuous OCR, build/watch processes or additional queue/broker is required for normal pilot operation. Use explicit container CPU/memory limits and measure idle polling, preparation/OCR, refresh/recovery and concurrent ordinary desktop use; the user evaluates noise. WSL2 global CPU/memory/swap settings affect other WSL workloads and must not be changed blindly (https://docs.docker.com/desktop/features/wsl/best-practices/; https://learn.microsoft.com/en-us/windows/wsl/wsl-config). Raw processing temp/spool/swap/host persistence still needs qualification under AD-6, not a false guarantee from tmpfs alone. Exact caps are qualification results, not demonstrated safe values.
- (direction) AD-13 revised PROPOSAL startup and outage: keep the host powered, network-connected and out of sleep/hibernate while serving; screen lock/display-off is compatible with an already running signed-in session. Configure Tailscale unattended with persistent Serve; Docker Desktop starts at Windows sign-in, then eligible created Compose services restart under unless-stopped, use persistent PostgreSQL volume, validate schema readiness and retry database connection. Backend resumes bounded polling/cleanup with stored source state; it cannot invent missed source updates or mark expired continuity fresh. Readiness includes backend/database and separate last successful source status. Planned restarts/releases occur between duties; an unexpected full Windows reboot can keep backend unavailable until Windows sign-in. Do not promise unattended cold-boot recovery from Docker Desktop or solve it silently with automatic Windows login. If recovery without any sign-in is mandatory, select and test a boot-managed Linux runtime or paid Linux host in a separate explicit decision. PC/home-network/Tailscale failure triggers the same prepared-day offline behavior; recovery requires successful sync/source refresh. Before actual-shift pilot test mobile HTTPS/access isolation, locked desktop, daemon restart, full Windows reboot with and without sign-in, home-network loss/recovery, quiet operation and offline continuation. No test has passed yet.
- (question) Next AD-13 user decision: recommend desktop plus private Tailscale Serve instead of a public browser-only tunnel; explicitly ask whether installing the Tailscale client on PC/tablet is acceptable and whether the first pilot may recover after Windows sign-in following a full reboot, or must recover unattended without sign-in. Do not mark the external-access/startup proposal adopted before this choice. The user's desktop-first/offline/fallback constraints are already adopted. Controlled releases/Service Worker and schema compatibility remain a subsequent decision point; avoid burying them in the external-access choice.
- (decision) AD-13 user steering [ADOPTED]: evaluate Cloudflare Tunnel as the first pilot access solution, replacing the prior Tailscale-first recommendation. Use separate hostnames and isolated data: private work application for the owner with additional restricted access beyond its own login, and an ordinary-browser demo for the teacher containing only fictional shifts. Investigate specifically whether the added access layer preserves AD-10 active-day continuation. Tailscale remains the fallback if Cloudflare becomes too elaborate. Windows desktop remains first pilot host; a possible inexpensive always-on Linux machine is unconfirmed and is not a V1 dependency. Keep Docker Compose portable across hosts.
- (decision) AD-13 restart envelope [ADOPTED by user]: the first Windows desktop pilot may require Windows sign-in after a full reboot before Docker Desktop and the application recover. Do not present unattended full-boot recovery as achieved or required for this first pilot. If a Linux host is acquired, configure and test boot-managed runtime/application startup there. Existing prepared-day offline continuity and moderate-resource/acceptable-noise requirements remain binding. This resolves the prior startup-choice question; Cloudflare Access session/continuation/isolation design still awaits a concrete interactive decision.
- (version) AD-13 Cloudflare evidence checked 2026-09-23: cloudflared latest stable release is 2026.9.1 dated 2026-09-11 (https://github.com/cloudflare/cloudflared/releases/tag/2026.9.1), seed to recheck/pin at implementation. A named Tunnel needs a Cloudflare account, domain and internet-connected origin, uses outbound connections and maps separate public hostnames to separate services (https://developers.cloudflare.com/tunnel/get-started/; https://developers.cloudflare.com/tunnel/). The current Zero Trust Free plan covers up to 50 users; domain registration/renewal and desktop electricity remain separate costs and no specific domain/provider purchase is selected (https://www.cloudflare.com/plans/). Do not use temporary Quick Tunnel URLs as the durable application origin. No Cloudflare account/domain/tunnel or executable configuration was provisioned.
- (version) AD-13 Access evidence: application/policy session durations and global identity session durations are independent, with configurable maximum one month and default 24 hours; an application token may be refreshed when the global identity token remains valid and policy still passes, otherwise reauthentication is required (https://developers.cloudflare.com/cloudflare-one/access-controls/access-settings/session-management/). Therefore neither an app day grant nor setting Access to fourteen days guarantees the other layer's continued authorization. The signed Access application JWT includes aud/iss/sub/email/exp, with exp an actual expiry timestamp (https://developers.cloudflare.com/cloudflare-one/access-controls/applications/http-apps/authorization-cookie/application-token/). Validate Cf-Access-Jwt-Assertion signature/audience/issuer/time before trusting claims; rotating signing keys require bounded refresh/failure handling (https://developers.cloudflare.com/cloudflare-one/access-controls/applications/http-apps/authorization-cookie/validating-json/). Cloudflare recommends creating Access protection before publishing the route; per-host Protect with Access can validate JWTs at cloudflared (https://developers.cloudflare.com/cloudflare-one/access-controls/applications/http-apps/self-hosted-public-app/; https://developers.cloudflare.com/api/resources/zero_trust/subresources/tunnels/).
- (version) AD-13 Access login/logout/browser evidence: email one-time PIN can be configured for an explicitly allowed email address; OTP is not automatically enabled for new organizations (https://developers.cloudflare.com/cloudflare-one/integrations/identity-providers/one-time-pin/). An Allow policy containing only the OTP login method would admit all valid emails, not just the owner (https://developers.cloudflare.com/cloudflare-one/access-controls/policies/). Application-cookie HttpOnly/SameSite settings must be explicitly checked; Strict may cause redirects, and Brave cookie protections can affect Access authentication (https://developers.cloudflare.com/cloudflare-one/access-controls/applications/http-apps/authorization-cookie/). Browser fetch can encounter login redirects/CORS failure instead of a structured backend 401; no generic 401-only claim is justified by Cloudflare One Client or Service Auth-specific options (https://developers.cloudflare.com/cloudflare-one/access-controls/applications/http-apps/authorization-cookie/cors/; https://developers.cloudflare.com/cloudflare-one/troubleshooting/access/). Access logout is independent of app logout, can affect all Access applications, and provider documents 20-30 seconds before all previously issued tokens stop being accepted (https://developers.cloudflare.com/cloudflare-one/access-controls/access-settings/session-management/). Documentation is not proof of Lenovo/Brave behavior.
- (direction) AD-13 Cloudflare topology PROPOSED, awaiting user: stable private hostname app.<owned-domain> and independent demo.<owned-domain>; these are logical address placeholders, not provisioned addresses. Named Cloudflare Tunnel runs as a Compose cloudflared service alongside private-web, demo-web (fictional static build), api (single FastAPI monolith) and db (PostgreSQL18). Explicit per-host ingress routes and a rejecting catch-all; private route requires the expected Access application, demo route serves only demo content. Private web and /api share one browser origin. No pilot database/API/HTTP host ports are published to LAN/internet; connect services on limited Docker networks, keep demo outside private API/database networks, and require private host/API authorization so the demo route cannot be repurposed as a private bypass. Demo has no real import backend, credentials, operational volumes or private backend routes; fiction runs client-side using shared operational code and separate origin storage. No credentialed demo-to-private CORS; host-only app cookies, exact Origin/CSRF checks and browser-origin isolation remain mandatory even when subdomains are same-site.
- (direction) AD-13 extra access PROPOSED: one exact-host self-hosted Access application protecting the entire private hostname, including its API; allow only the explicitly selected owner identity, with email OTP as the minimal candidate instead of a new broad user directory. Provision Access before enabling the private tunnel route. Enable per-private-host Access JWT enforcement at cloudflared and validate signed private application identity/expiry at the backend boundary when used for preflight. Keep Cloudflare credentials out of frontend/IndexedDB and preserve AD-10 backend login/ownership/day/writer authorization independently. No browser service token, unprotected API path, demo-origin relay or bypass policy to work around expiry. Demo remains public no-login fictional access under the adopted UX/AD-10, not part of the private Access application or wildcard cookie scope.
- (direction) AD-13 AD-10 compatibility PROPOSED: use a one-month Access application/policy session and compatible global session as the initial pilot setting, with no shorter hidden policy/MFA/client override. This is a separate outer admission lifetime, not a replacement for general_until=app_auth_at+14 days, day_grant_until or settlement_until. During backend-authorized whole-day preparation, inspect the current VERIFIED Access JWT expiry and require it to cover the specific day grant including its bounded post-day synchronization window, with an implementation-tested clock margin. If it does not, the user must renew Access before marking online continuity ready; confirm the returned token actually has sufficient remaining life rather than assume SSO refresh extended it. App confirmation/offline data readiness and outer access readiness are distinguishable; existing local work/manual corrections are not erased or stopped merely because Access needs renewal. Any later authorized grant extension repeats this check. This prevents ordinary Access timeout predictably interrupting the authorized day, but cannot guarantee connectivity, prevent deliberate revocation or preserve cookies the browser removes.
- (direction) AD-13 continuation and failure PROPOSED: Access renewal only restores passage through the outer gate; it never renews the app's fourteen-day session, creates a day grant, changes writer_epoch, resumes an ended day or authorizes a new day. After normal app expiry, a valid day grant still scopes backend actions to that day. A loaded prepared client continues under AD-2/AD-10 during Access expiry/unavailability; source updates and sync pause and the UI identifies an access/connectivity problem without presenting stale data as fresh. No automatic full-page login redirect while driving. Renew Access through deliberate navigation when interaction rules permit, then retry the unchanged idempotent batch after receiving valid backend responses. Treat redirects, HTML login pages, unexpected content types and opaque network/CORS failures as NOT synchronization receipts and never cache them as the application shell or delete outbox entries because of them. Expired admission is not a known backend revocation; confirmed revocation/logout still locks according to AD-10. Browser recovery after closure with expired Access must use the verified cached shell/day and must be qualified on Lenovo/Brave.
- (direction) AD-13 logout with Access PROPOSED: online app logout durably locks local use, sends backend session/day-grant revocation first while the gate is still usable, then attempts Access logout; report each pending outcome honestly and do not promise instantaneous provider-wide revocation. Offline logout retains the pending server revocation and protected unsynced work under AD-10/AD-12. If Access blocks the revocation request after reconnect, explicitly report that server logout still waits for Access renewal; any necessary Access authentication is prerequisite control traffic while the app stays locked. Once admitted, backend revocation must precede all other private application traffic. A Cloudflare login by itself must not unlock the pending local work; recovery still needs renewed app login by the same owner after the pending old logout is settled. Do not create a public unauthenticated logout bypass or silently clear the queue.
- (direction) AD-13 transit/cache PROPOSED qualification: Cloudflare terminates browser TLS and handles private requests, including raw shift uploads, before the encrypted tunnel to the desktop; it becomes part of the processing/trust boundary, not an opaque end-to-end channel to the desktop (https://developers.cloudflare.com/ssl/concepts/; https://developers.cloudflare.com/tunnel/). Explicitly bypass edge caching for the private hostname, send no-store on private/API/auth responses, disable archival/Always Online or body-capture features for private routes, and never log private payloads/raw OCR/credentials. Cache only verified nonpersonal app shell in the browser under AD-2; no challenge/login/API cache poisoning. Cloudflare documents no-store and bypass controls (https://developers.cloudflare.com/cache/concepts/cache-control/; https://developers.cloudflare.com/cache/how-to/cache-rules/settings/). Inspect provider request/security logging and upload handling before real files; local deletion does not prove erasure of provider operational metadata. Keep identity/access metadata separate from workday data and avoid personal operational information in URL paths/query strings. If the accepted raw-data/retention rules cannot be met, revisit the access solution (Tailscale fallback) rather than silently weaken AD-6/AD-12.
- (direction) AD-13 portable operations PROPOSED under already accepted Windows restart limit: use the same versioned application images/Compose service graph and environment configuration on Windows Docker Desktop Linux containers and a later Linux host; named working volumes, explicit external secret files, no hardcoded Windows host paths. cloudflared is an ordinary long-running Compose service with restart policy, so its availability shares the accepted Windows sign-in dependency. Use restart unless-stopped for eligible long-running services, schema/readiness checks and backend database reconnect; source outage is separate from process readiness. Later Linux boot starts Docker and the Compose workload through tested service management, without making Linux required for V1. Preserve a stable browser hostname when moving hosts if practical; do not run two independent operational databases behind one active tunnel, and plan any current-data migration with retention and writer authority intact. CPU/memory bounds, one OCR at a time, no continuous builds/watchers, quietness tests and prepared-day offline fallback remain proposed pilot conditions. No historical private-data backups under AD-12.
- (question) Present AD-13 concrete choice: desktop-first portable Compose; one named Cloudflare Tunnel; exact private hostname with owner-only Access plus app login; isolated fictional public demo hostname. Recommend one-month outer Access with actual-expiry preflight covering the already bounded day grant, independent app fourteen-day scope and access-renewal/logout ordering. Disclose that Cloudflare is a private-traffic intermediary and that this is documentation-backed design, not a tested deployment. Domain/account/allowed email/cost specifics belong to provisioning before the pilot, not an invented address or present purchase. Require target tests for expired app session with valid day grant, Access expiry/renewal without granting new-day access, blocked offline logout, cold browser restart offline/expired gate, private/demo isolation, non-JSON auth replies and lost sync receipts, whole Windows reboot/sign-in, resource/noise and home-network failure. User's Windows sign-in and optional Linux decisions are already adopted; do not re-ask those. After AD-13 choice, proceed to controlled releases/schema/Service Worker compatibility and final evidence gates.
- (decision) AD-13 [ADOPTED] - Desktop-first portable Compose, Cloudflare Tunnel/Access and isolated fictional demo. Binds: deployment origins, ingress/network isolation, access boundary, authorization readiness, host startup and pilot qualification. Prevents: a public demo reaching private data, outer access expiry silently overriding active-day continuity, and a host-specific setup blocking later migration. Rule: adopt the presented Cloudflare proposal: separate exact private/demo hostnames, owner-restricted Access ahead of independent app login, named outbound Tunnel, private API/database ports and networks, fictional static demo with no private API/data/credentials, portable versioned Compose services on Windows desktop first. Access lifetime stays independent of app general access and bounded day grants; actual verified expiry is checked during preparation, with clear renewal and logout ordering. Tailscale and a later Linux/paid host remain alternatives, not V1 prerequisites. Windows sign-in after full reboot is an accepted first-pilot limitation. Rationale: ordinary-browser access for both pilot user and teacher without a tablet VPN dependency, while keeping private authority in the application.
- (constraint) AD-13 final user clarification [ADOPTED]: when remaining Cloudflare Access lifetime is too short at preparation, the application must show this clearly and guide renewal without pretending that Access preflight guarantees internet availability for the whole day. Distinguish verified offline readiness, currently reachable services and credential expiry coverage; never label a day as guaranteed online. Test Access renewal, expiry and continuation offline on the actual Lenovo/Brave setup before the pilot. The previous one-month starting setting and actual-expiry comparison with the bounded day allowance remain adopted; no test success or external provisioning is implied.
- (question) AD-13 adopted. Next AD-14 is controlled application/backend releases and storage/API compatibility: prepared active-day recovery must survive old clients, browser restart, immutable pending batches, local IndexedDB migration and Service Worker activation. No automatic destructive reset or forced mid-day reload. Need a concrete user choice for scheduled releases, preservation of compatible cached builds, a bounded compatibility lifetime tied to existing data expiry, migration failure behavior and rollback without forbidden historical private-data backups. Final target-device/source/import evidence gates remain after that; do not silently finalize the workflow.
- (version) AD-14 update evidence checked 2026-09-23: a new Service Worker normally waits only until the old worker no longer controls clients, so closing all tabs can permit activation even while an application-level working day remains active; skipWaiting can cause old pages/new worker combinations (https://web.dev/articles/service-worker-lifecycle; https://web.dev/learn/pwa/update). Avoiding forced reload alone does not enforce an active-day update boundary. IndexedDB schema changes use versionchange transactions and open connections can block them (https://developer.mozilla.org/en-US/docs/Web/API/IndexedDB_API/Using_IndexedDB; https://developer.mozilla.org/en-US/docs/Web/API/IDBOpenDBRequest/blocked_event). PostgreSQL transaction semantics support all-or-nothing changes for suitable migration operations (https://www.postgresql.org/docs/18/tutorial-transactions.html). This is documentation evidence only; no update/migration/rollback test has run.
- (direction) AD-14 PROPOSED release policy, awaiting user: use explicit versioned releases with pinned application/container artifacts and dependency locks; record a build/release identifier separately from API/event schema versions, local database schema version and AD-5 plan/server revisions. Plan ordinary releases between workdays, checking pending work/conflicts and synchronizing when possible first. Download/stage a new static app bundle without activating it mid-day; switch the operational application only at a controlled boundary, normally after the day is ended and the user accepts the update. No automatic live replacement, forced reload or destructive reset while driving. Short backend maintenance is handled by the accepted offline mode, without claiming uninterrupted source delivery. Compare with unconditional latest-version activation: it is simpler but can strand the very local state and batches offline operation must preserve; prefer controlled upgrades for this single-user pilot.
- (direction) AD-14 PROPOSED active-day client consistency: persist the client build required by an active day/recovery state, keep the complete verified nonpersonal asset set available in its versioned cache, and preserve recovery into that version across browser closure/restart while local storage remains available. Service Worker installation/activation is not itself permission to change the day's application version or local schema. New worker/bootstrap routing must respect this active build and not mix unrelated HTML/JavaScript or delete assets it still needs. Fully stage/verify the next build before switching; leave the previous usable build selected on failed/partial download or validation. Retain necessary code, not duplicate private historical data. Exact manifest/cache routing implementation is a story detail; no guarantee against browser eviction/clearing is introduced. Coordinate writing tabs/workers and migrations under AD-11 before switching, and keep access/retention guards effective. Future critical security fixes require an explicit controlled recovery/stop decision if normal compatible update cannot be used; they are not a silent force-refresh exception.
- (direction) AD-14 PROPOSED bounded compatibility: prefer additive V1 API/schema changes. A new backend must still accept the previously supported immutable event/batch formats needed by unexpired already-authorized days, and the new client must be able to preserve/recover retained drafts, history and pending work. Tie support/removal to the actual AD-12 expiry of affected records/grants, not an arbitrary last-two-releases rule or seven days from deployment. An old client can be required to upgrade before authorizing/preparing a new day; this does not revoke an existing valid day's continuation. Auth, writer ownership, revision and expiry checks always apply; compatibility never recreates an expired day. Unknown/unsupported schemas return the explicit AD-5 failure outcome without partial writes or dropped local work. Failed compatibility is an implementation defect/blocker to release, not permission to shorten the granted retention/continuation window.
- (direction) AD-14 PROPOSED data migration and rollback: run versioned, tested IndexedDB/PostgreSQL migrations with atomic transactions where supported and an explicit validated recovery path for any multi-step exception. Keep immutable submitted/pending batch identifiers, schema and payload meaning intact; never rewrite an already sent batch under the same idempotency key to fit a new format. Preserve manual choices, role/context/pin, notice-version receipt/ack/hidden states, pending logout/revocation, writer_epoch, deadlines and uncertainty. If local migration is blocked by another tab or fails, show the reason and preserve existing data/usable compatible build; do not solve it by clearing IndexedDB, cache or outbox. Retained-data copies/temporary migration records remain within the same deletion scope, and migrations do not renew deadlines. App rollback is permitted only against a tested compatible storage schema; do not assume old binaries can read a newer schema or run destructive automatic database downgrades. AD-12's no historical private-data backups and possible permanent data loss remain unchanged; schema migration is not a disaster-recovery guarantee.
- (direction) AD-14 PROPOSED minimum release evidence: an old client with an unexpired prepared day and pending immutable batch must synchronize correctly after the server update, including lost receipt/retry and conflict handling; browser close/reopen during active work must restore a consistent cached build/state offline and with Access expired; partial asset download must not switch to a broken app; open second tab must not cause silent state loss during migration; simulate interrupted/failed migration and verify recoverable prior state, expiration guards and pending logout order. Use PostgreSQL for backend migration/integration tests, fictional fixtures and no production database copies. Exact tools and scripts are story details, not new infrastructure components. Access/source/GPS/import/pilot qualification from prior decisions remains mandatory and unproven.
- (question) Present AD-14 for approval as controlled releases between days, active-day cached application continuity even after browser restart, backward compatibility through the affected data's existing expiry, non-destructive migrations with immutable pending batches, and code rollback only when storage-compatible. User should approve this consequential update policy before final distillation; do not mark AD-14 adopted yet. After this, consolidate outstanding validation gates and deliberate implementation deferrals, then distill/reconcile/review the spine according to the skill without silently introducing new architecture choices.
- (decision) AD-14 [ADOPTED] - Controlled releases and compatibility through data expiry. Binds: client boot/Service Worker/cache, local storage migration, API/event compatibility, deployment releases and rollback. Prevents: a new build stranding an active day or immutable pending batches, mixed-version recovery and destructive update repair. Rule: adopt the presented AD-14 update model: maintain one coherent application version for an active day through ordinary close/reopen, stage and validate complete releases before controlled activation between days, and preserve support for its event formats through the affected data's existing deletion deadline. Migrations preserve pending work/authority/manual choices/deadlines and fail without destructive resets; code rollback requires compatible storage. Existing expiry, ownership and revocation checks remain binding. Rationale: a prepared offline day must remain recoverable while server and browser versions evolve independently.
- (constraint) AD-14 final user clarification [ADOPTED]: specifically test closing and reopening while an update waits. If required files are missing, show a clear error and preserve local private data; never start with an arbitrary mixture of old/new code. A minimal consistent recovery/bootstrap path must report missing assets without silently resetting work. Keep implementation as simple as possible for V1: one active build and a staged successor as the normal client case, bounded retained formats/assets only when existing work requires them, no generic release-orchestration platform. Browser storage remains evictable; no update policy promises recovery of files/data the browser has actually deleted.
- (event) All fourteen coached architecture decisions have been adopted. Begin final distillation, input reconciliation and the configured independent reviewer gate. Preserve the user-requested concise English Architecture Spine with brief rationale and essential diagrams; any new load-bearing decision discovered by reconciliation/review must return to the user. Existing source/device/import/access/retention tests remain future evidence gates, not architecture-run claims. This is document finalization only, not application scaffolding or deployment.
- (event) Distilled all fourteen adopted decisions into the English Architecture Spine with six conceptual contracts, operational/deployment diagrams, version evidence and explicit unproven pilot gates. Independent brief, PRD and EXPERIENCE reconciliation found only existing-requirement clarifications: stale notice retention/source links, missing-stop manual completion, actual retrieval acceptance, repeatable fictional demo, context-bound pinning and revision target/link handling. Applied these without adding a new decision. Initial deterministic lint passed with zero findings; DESIGN reconciliation and configured reviewer gate follow.
- (event) DESIGN reconciliation passed with one inherited qualification clarification: actual mounted-tablet readability, contrast, responsive fit and touch/glove evidence remain required. Applied it. All four authoritative input packages reconciled; final draft has AD-1 through AD-14, six conceptual contracts and three diagrams. Deterministic lint passes with zero findings and local document links resolve. Running the required independent rubric, technology-evidence and adversarial-consistency reviewers before finalization.
- (decision) Final reviewer clarifications within adopted ADs: AD-14 compatibility includes the retained client required responses/errors/recovery/notices/access/logout, not only event input. AD-11 planned/emergency writer transfer atomically rebinds the client/session day grant, retires the former grant and preserves deadlines. AD-5 authorizes receipt lookup independently from current writer authority needed for new mutations; restore one stable in-flight batch per day and separate source polling revisions. AD-12 immediate privacy trimming uses a closure-only minimal retained recovery checkpoint when pending payloads are affected: retire old payloads/IDs without treating this as acknowledgment, reconcile payload-free receipts/status, submit new checkpoint batch/event IDs with current epoch/revision, atomically fence unaccepted old IDs and perform terminal cleanup, preserve checkpoint on conflicts and unchanged retry on lost settlement response. Accepted old receipts remain readable under authorization; unaccepted fenced originals return batch_retired; terminal validation blocks fresh-ID resurrection. These close implementation seams without changing approved scope, deadlines, data-loss choices or explicit conflict rules. No new product/architecture alternative remains.
- (event) spine finalized. All AD-1 through AD-14 are adopted and distilled. Product Brief, PRD, EXPERIENCE and DESIGN packages independently reconciled; inherited omissions corrected. Configured rubric, current-technology evidence and adversarial consistency reviewers passed after targeted verification of fixes, with no outstanding architecture decision. Final deterministic lint reports zero findings; relative document links resolve. ARCHITECTURE-SPINE.md status is final, updated 2026-09-23. Source coverage, OCR qualification, Lenovo/Brave behavior, Cloudflare/access, desktop operations, retention and release compatibility remain explicit unproven implementation/pilot gates. No application code, external service provisioning or deployment performed. Recommend adopting the spine as a BMAD Spec companion, then Epics & Stories with stable AD references.
- (event) User closed step 4 and directed the handoff: update Docs/development-log.md, record AD-1 through AD-14 as adopted and pilot prerequisites as still requiring tests, then commit and push relevant project documentation on the current branch. Step 5 is Epics & Stories. This supersedes the earlier optional BMAD Spec recommendation; neither BMAD Spec nor Epics & Stories is started in this session.
